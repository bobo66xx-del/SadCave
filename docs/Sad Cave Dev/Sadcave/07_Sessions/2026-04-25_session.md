# Session — 2026-04-25

> First real session — designed the workflow, set up the vault scaffolding, prepped for repo resync. No code shipped this session; this was infrastructure work.

## ✅ Built / Shipped
- Designed the full Opus + Codex + Vault + Studio + Rojo workflow
- Created `00_Inbox/_Inbox.md` (shared three-way capture surface)
- Created `_Change_Log.md` (append-only history at vault root)
- Created `09_Open_Questions/_Open_Questions.md` (persistent unresolved decisions)
- Created `_Workflow.md` (full workflow doc + re-orientation checklist)
- Created `_Codex_Instructions.md` (now superseded — see Decisions below)
- Updated `00_Index.md` with new Workflow & Capture section

## 🐛 Broke / Fixed
- Initially put open questions at `04_Open_Questions/` — collided with existing `04_Dialogue/`. Moved to `09_Open_Questions/`.
- Vault was briefly moved out of `docs/` folder, then moved back. No data lost. Path stayed `C:\Projects\SadCave\docs\Sad Cave Dev\`.

## 🤔 Decisions
- **Stack roles:** You direct, Opus designs, Codex implements, vault is shared spec.
- **Inbox:** Three-way capture, prefixes `[U]` / `[O]` / `[C]`, empties at session end.
- **Change log:** Append-only, only substantive shipped changes, written by Opus during integration.
- **Integration cadence:** End of every session by default.
- **Codex review:** Opus reads Codex's output before integration and flags drift from spec.
- **Codex write boundaries:** Codex writes only to `00_Inbox/_Inbox.md` and the `Status` field of its current plan file. All other vault edits go through Opus.
- **Stay on Codex** (not Claude Code) — $100/month flat is cheaper than projected API spend.
- **Don't scrap the Rojo repo** — it's stale, not broken. Resync from Studio.
- **AGENTS.md is the right home for Codex rules**, not a vault file. To do next session: migrate `_Codex_Instructions.md` content into `AGENTS.md` at the repo root, delete the vault file, update `_Workflow.md` and `00_Index.md` references.

## ▶️ Next Up

**1. Repo connection (you do this before next session):**
- Add filesystem MCP entry to `claude_desktop_config.json` pointing at `C:\Projects\SadCave`
- Server name suggestion: `sadcave-repo`
- Fully quit and restart Claude Desktop
- Start a fresh chat — this session's tools won't carry over

**2. First task in fresh session — re-orient:**
- Tell new Opus: "read `_Workflow.md` first, then this file"
- Confirm `sadcave-repo` MCP is connected

**3. Then — migrate Codex instructions to AGENTS.md:**
- Opus reads `_Codex_Instructions.md` from the vault
- Opus drafts `AGENTS.md` content (you create the file in repo root since it lives outside the vault)
- Opus deletes `_Codex_Instructions.md` from vault
- Opus updates `_Workflow.md` and `00_Index.md` to point at `AGENTS.md` instead

**4. Then — repo resync planning:**
- Opus reads the existing repo (now accessible via filesystem MCP)
- Opus reads relevant Studio state via Roblox MCP
- Opus reads relevant `02_Systems/` notes
- Opus writes `06_Codex_Plans/2026-04-25_Repo_Resync_v1.md` — the actual plan Codex will execute
- Plan must cover: backup steps (branch + .rbxl save), folder structure, sync method, cleanup decisions for legacy scripts (Shop, CashLeaderstats, DonationLeaderstats, duplicate SoftShutdowns, duplicate Purchases)

**5. Codex executes the resync plan locally.**

**6. Cleanup pass + first real `_Change_Log.md` entry.**

## 📝 Notes

- **Active focus this week** in `00_Index.md` is still empty (`_(fill in what you're working on)_`). Worth filling in once resync is done — likely "Repo resync + cleanup" first, then the original priority (XP/Progression + map expansion).
- Studio script tree was partially scanned this session — saw ~50+ scripts in ServerScriptService alone, confirmed cleanup backlog items (duplicate SoftShutdowns, duplicate Purchases, Shop, CashLeaderstats, DonationLeaderstats, DonationAmount) are present and real.
- Vault and repo are co-located under `C:\Projects\SadCave\` — vault at `docs/Sad Cave Dev/`, repo at the root. Decision to gitignore vault vs. commit it (or hybrid: commit markdown, ignore `.obsidian/`) is still open — flag for later.
- Did not yet scan StarterPlayerScripts, StarterGui, Workspace, or Lighting — those will come in resync planning.
