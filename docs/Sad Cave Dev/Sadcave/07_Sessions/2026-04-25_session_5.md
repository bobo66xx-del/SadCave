# Session — 2026-04-25 (session 5)

## ✅ Built / Shipped
- No code shipped this session — workflow/tooling pass only.
- AGENTS.md restructured: merged `_Workflow.md` into it, added Git Workflow section, added playtest failure modes, dropped status emojis on plan files.
- New vault file: `02_Systems/_No_Touch_Systems.md` — canonical no-touch list, maintained alongside the systems instead of inline in AGENTS.md.
- GitHub MCP installed and verified working — Opus can now read pushed branches via GitHub MCP for review.

## 🐛 Broke / Fixed
- Docker installation hit two snags: (1) "must be owned by an elevated account" — fixed by deleting `C:\ProgramData\DockerDesktop` and running installer as admin. (2) "WSL needs updating" — fixed by `wsl --update` in elevated PowerShell, which prompted a self-repair.
- GitHub PAT was leaked in chat once during setup — revoked and regenerated immediately, no impact since the leaked token only had read-only scope on the SadCave repo.

## 🤔 Decisions
- **Source of truth correction:** the repo is canonical, Studio is the runtime (not the source of reality, as previously written). Locked into AGENTS.md.
- **Branch-and-merge workflow:** Codex works on `codex/<task-name>` branches and pushes them; user merges only after Opus reviews. Never pushes to `main` directly. New anti-pattern logged.
- **Universal Opus review:** every Codex task gets reviewed by Opus before user accepts — no risk gradient. Reflects that the user does not script and cannot verify code themselves.
- **One workflow doc, not two:** `_Workflow.md` and `AGENTS.md` collapsed into AGENTS.md. The pairing rule that existed to keep them in sync is no longer needed because there's only one doc.
- **Plan status emojis removed.** Codex no longer updates 🔵→🟡→🟢. Vault write access is now just the inbox.

## ▶️ Next Up
- Hand the XP Progression MVP brief to Codex for actual building (`06_Codex_Plans/2026-04-25_XP_Progression_MVP_v1.md`).
- Codex creates the `codex/xp-progression-mvp` branch, builds, pushes.
- New chat for Opus review (this conversation predates GitHub MCP, so review needs a fresh chat).

## 📝 Notes
- Setup phase is officially over. Tonight closed the door on workflow/tooling work; next session should be game work.
- Reminder for next session: when starting a fresh chat, Opus re-orients via `AGENTS.md` + `00_Index.md` + `_Inbox` + most recent session file. The vault is the project's memory between chats.
- Branch protection on `main` (GitHub Settings → Branches) is an optional future hardening — not required, but worth doing once comfortable with the merge flow.
