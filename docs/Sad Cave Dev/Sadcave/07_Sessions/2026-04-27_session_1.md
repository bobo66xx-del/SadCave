# Session — 2026-04-27 (session 1, Cowork)

First session under the Cowork desktop tool. Picks up from session 5 on 2026-04-25.

## ✅ Built / Shipped

- No code shipped. Full vault refresh and queued Codex brief.
- `06_Codex_Plans/2026-04-27_Repo_Strip_to_Studio_State_v1.md` — written and ready for Codex. Narrower than the chat-only draft from the prior session because verification showed the repo `src/` is already lean.
- Vault meta-docs heavily refreshed: `_Live_Systems_Reference`, `_UI_Hierarchy`, `_Cleanup_Backlog`, `_No_Touch_Systems`. Each now reflects post-2026-04-27 reality with explicit audit-trail sections of what was removed.
- Per-system specs updated with post-cleanup status: `XP_Progression`, `Level_System`, `Daily_Rewards`, `Area_Discovery`, `NameTag_Status`, `Title_System`, `Cave_Outside_Lighting`, `Level_Stat_System`. `00_Index.md` re-tiered.

## 🐛 Broke / Fixed

- Inbox claim from prior session ("None of these four new artifacts are in the repo yet") was incorrect. Verified that three of the four — `NameTagScript`, `AfkEvent`, `AfkDetector` — are in the repo via Rojo sync. Only `Workspace.ToolPickups` empty Folder is Studio-only (appropriate, since empty Workspace folders aren't typically Rojo-managed). Repo-strip brief was scoped accordingly.
- Studio MCP `inspect_instance` returns minimal data (just `instanceName`) for all queries this session, so live-state verification relied on confirming presence-only rather than reading children. Confirmed sufficient for the artifact check; flagged as something to test next session in case it impacts other workflows.

## 🤔 Decisions

- **Cowork migration confirmed.** Same MCPs wired up (Roblox Studio, GitHub, sadcave-repo, Obsidian) plus shell, structured task tracking, and persistent artifacts. No capability lost. AGENTS.md still reads "Opus (Claude, in chat)" — cosmetic update deferred until the next time AGENTS.md is edited for a substantive reason. Vault and repo are unchanged on this front; only the host changed.
- **Repo-strip brief is housekeeping, not redesign.** Confirms the three new artifacts are committed, retires `xp-only.project.json`, freezes or refreshes `docs/live-repo-audit.md`. Default to freeze with a banner unless a fresh audit is explicitly requested.
- **`Level_Stat_System.md` left as a stub** instead of deleted. The doc itself previously suggested deletion, but keeping it as a permanent pointer means old `[[Level_Stat_System]]` wikilinks don't break.
- **System docs that describe deleted systems are kept as "Historical / Superseded" entries**, not deleted. Same reasoning — the change log and other system docs reference them by name; nuking them would break the vault's link graph and lose the audit trail.

## ▶️ Next Up

- Address the 11 Studio-only kept-list objects Codex flagged in PR #2 (`DialogueData`, `DialogueRemotes`, `RemoveFF`, `DiscordLogs`, `Reset`, `SoftShutdown`, `PromptGroup`, `PromptFavorite`, `NpcDialogueClient`, `PlayerDialogueClient`, `environment change`). Either export each into the repo or document each as intentionally Studio-only — small follow-up brief.
- Resolve the two pending items from the original repo-strip brief: retire `xp-only.project.json` and freeze-or-refresh `docs/live-repo-audit.md`. Could be folded into the dialogue/utility export brief above.
- Walk the post-merge testing-place checklist from `09_Open_Questions/_Open_Questions` (sitting boost, level-up animation, gamepass +22 tick, mobile bar height, second-join migration, DataStore failure simulation).
- Pick the next XP follow-up brief: Discovery source, Conversation source, AchievementTracker, or jump to Title v2. Tyler's call based on which is most exciting.

## 📝 Notes

- **Five vault improvements adopted in the same session.** After the initial cleanup pass, Tyler asked for ideas to make the workflow more drift-resistant and approved all of them. (1) Session Bookends in `AGENTS.md` (Start / Reality Check / End checklists). (2) `_Review_Template.md` for the Codex review routine. (3) `01_Vision/Environments.md` for the testing-vs-production split. (4) `_Decisions.md` for forward-looking design decisions, separate from the change log. (5) Persistent Cowork artifact `sad-cave-dev-tracker` pulling live state from vault and GitHub.
- Surprise during the session: PR #2 (`codex/repo-strip-to-studio-state`) had already been merged at 10:13 UTC before this Cowork chat began — Codex executed against the prior chat's draft brief during a window I wasn't watching. The plan I wrote for the same task this session is therefore mostly redundant on the strip portion; the housekeeping pieces (`xp-only.project.json` retirement, `docs/live-repo-audit.md` resolution) and the 11 Codex-flagged Studio-only objects are still pending.
- Vault drift detected and flagged separately: `Workspace.SeatMarkers` exists but had 0 children at last test, so the sitting boost is unverifiable end-to-end until at least one `SeatMarker` is placed. Tracked in `_Open_Questions`.
- "Production caveat" added to `00_Index`, several specs, and now formalized in `01_Vision/Environments.md`. The testing place is now post-cleanup; production may not be. Production cutover gets its own dedicated brief per the Environments doc's protocol.
- Cowork artifact ergonomics: structured task list rendered as a widget; persistent artifacts shipped this session for the dashboard. Worth probing what other artifacts would help next session — maybe a per-system status detail page if drift recurs.
