# Session — 2026-04-27 (session 2, Cowork)

Second Cowork session of the day. Picks up after session 1 + the morning's PRs #1–#5. Focused on workflow hardening (Tyler caught sync drift in the index/change log/inbox) and shipping the audit refresh.

## ✅ Built / Shipped

- **PR #6 — Live Repo Audit Refresh** (merged 2026-04-27 20:51 UTC). 49 rows classified across the cleaned testing place: 27 Exact / 0 Structural / 5 Studio-only / 10 Manual export needed / 7 Tooling blockers. Reviewed: spot-checked 3 Exact entries (`RemoveFF`, `AfkDetector`, `LevelCurve`) byte-equal, no webhook leaks, DiscordLogs handled correctly. Replaces the frozen banner left by PR #4.
- **Workflow tightening** in `AGENTS.md`: new Opus inbox-first rule (any vault edit needs an `[O]` line first), Start-of-Session step 5 querying GitHub for PRs since last recap, End-of-Session step 6 validating plan/index/change-log consistency. Plan-file `Status:` convention adopted across all `06_Codex_Plans/` files. Change-log format hardened to require `HH:MM UTC` timestamps and event-time sorting.
- **Vault reconciliation** of three drift items the audit caught: `Environments.md` Studio id `b2f076e7-...` → `b00d97e0-...`; `_UI_Hierarchy.md` updated to reflect `IntroScreen` + `Menu` still live + new `Game Version` discovered (count 2 → 5 source-bearing ScreenGuis); `_Live_Systems_Reference.md` updated to reflect `SeatMarkers` now has a `Seat` + `CustomSitAnimScript` (was 0 children).

## 🐛 Broke / Fixed

- **Change log was out of order + had a prepend-bug.** Mid-session edit fused the PR #3 entry with leftover text from the Housekeeping brief entry. Resort by event time + de-mangling fixed it. Also backfilled missing entries for PR #1 and the pre-Cowork cleanup pass that were only ever in the inbox.
- **Inbox went silent for `[O]` writes during session 2** until Tyler caught it. Root cause: convention had a soft edge for design/planning work where there's no obvious "observation." Fixed by tightening `AGENTS.md` Section 4 Capture with an explicit Opus rule.
- **Index drifted from reality** (still queueing the repo-strip brief that shipped). Refreshed Current Priority + Recently Completed + Plans & Logs to match. Added validation step to End-of-Session bookend so it can't drift cross-session again.

## 🤔 Decisions

- **Workflow sync hardening** (`_Decisions.md` 2026-04-27). Three new rules: Opus inbox-first, Start-of-Session GitHub sync, End-of-Session consistency validation. Each turns a sync failure mode into a checkable invariant. Validation script (Tier 2) deferred unless manual checks fail twice.
- **Change-log format hardening** (`_Decisions.md` 2026-04-27). Use `YYYY-MM-DD HH:MM UTC — [System] — ...` when timestamp known, `(<session label>)` otherwise. Sort newest-on-top by event time, not write time.
- **Plan-file Status convention** (`_Decisions.md` 2026-04-27). Every `06_Codex_Plans/` file carries a `**Status:**` line: 🔵 Queued / 🟡 Building / 🟢 Shipped / ⏸ Waiting / ⚫ Superseded.

## ▶️ Next Up

- **XP testing-place checks.** Sitting boost (now unblocked since SeatMarkers has a child), level-up animation, gamepass +22 tick, mobile bar height, second-join migration, DataStore failure simulation. Cheapest first; Tyler and Opus walk it together via Studio MCP.
- **UI keep/delete decisions.** `IntroScreen`, `Menu`, `Game Version` — drift caught by the audit. Each needs a Tyler call before any export/cleanup.
- **Manual Export queue follow-up brief.** `Workspace.Avalog` is load-bearing (453-script dependency of `FavoritePromptPersistence`); the rest (`Rose`, `Leader2`, `playerBugReportSystem`, `ReportGUI`, `Truss`, `WelcomeBadge`) can group into one brief.
- **DiscordLogs Secret Refactor.** Brief exists, ⏸ Waiting on Tyler's choice of secret-handling approach (Branch A/B/C in the brief).
- **Pick the next XP follow-up.** Discovery source, Conversation source, AchievementTracker, or jump to Title v2.

## 📝 Notes

- **Three pre-existing bugs surfaced during PR #4/#5 playtests** moved to `_Known_Bugs.md`: `FavoritePromptPersistence` line 4 SourceCode error (likely related to the Avalog dependency the audit flagged), `PromptFavorite` infinite yield on `FavoritePromptShown`, and `PromptFavorite` class mismatch (live class is `Script`, exported as `.client.lua`). All low-priority; the class mismatch wants a small Codex brief eventually.
- **AGENTS.md still reads "Opus (Claude, in chat)"** — cosmetic update from session 1's deferral list still hasn't happened. Should fold into the next AGENTS.md edit pass that touches it for a real reason.
- **Codex's "drift flags" output during the audit was the most valuable part of PR #6.** The audit doc itself is a snapshot, but the drift findings (Studio id, IntroScreen/Menu, SeatMarkers child) revealed three places where the vault was lying about reality. Worth keeping the "Drift Flags For Inbox / Review" section as a standard part of any future audit format.
- **Tyler is moving to a new chat after this wrap-up.** Next session's Start-of-Session re-orient should land cleanly: AGENTS.md has the new bookend rules, index is current, change log is sorted, plan files have status, inbox will be cleared (after this), session recap exists.
