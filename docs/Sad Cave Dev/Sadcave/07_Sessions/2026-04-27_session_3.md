# Session — 2026-04-27 (session 3, Cowork)

Third Cowork session of the day. Picks up after session 2's wrap-up landing on main as PR #7. Focused on resolving outstanding decisions from the audit refresh and shipping the PromptFavorite bugs cleanup.

## ✅ Built / Shipped

- **PR #8 — PromptFavorite Bugs Cleanup** (merged 2026-04-27 22:00 UTC). Bounded the unbounded `WaitForChild("FavoritePromptShown")` in `PromptFavorite.client.lua` to 10 seconds with a tagged graceful-return warn if the remote is missing; deleted the duplicate `StarterPlayer.StarterPlayerScripts.PromptFavorite` `Script` instance in Studio after Codex confirmed its source matched the canonical `LocalScript` modulo one trailing newline. Reviewed against the brief; skipped independent playtest because Codex's notes were concrete and the change is tiny / client-only / no DataStore or remote-contract surface. Tyler merged.
- **PR #7 — Session-2 vault wrap-up** (merged 2026-04-27 21:54 UTC). 18 vault files including AGENTS.md sync rules, plan-file Status convention, `_Decisions.md` introduction, change-log reorder + per-PR HH:MM UTC backfills for PRs #1–#6. Tyler had authored the local commits during session 2; PR #7 is what landed them on main. Logged once in `_Change_Log.md` so the merge event is captured; no other vault change needed (the content was already correct locally).
- **Vault integration of session 3 decisions:** `_Decisions.md` got four new entries (UI keep, Workspace skip, Title v2, brief equality whitespace-tolerant); `_UI_Hierarchy.md` drift section moved from "decision pending" to "kept by Tyler 2026-04-27"; `_Open_Questions.md` resolved three items; `_Known_Bugs.md` moved two PromptFavorite bugs to Resolved; `docs/live-repo-audit.md` marked the duplicate `PromptFavorite` Script tooling blocker resolved; `00_Index.md` Plans & Logs picked up the new brief and Current Priority got refreshed.

## 🐛 Broke / Fixed

- **Brief equality rule fired on a one-trailing-newline diff.** First-pass run had Codex stop at step 2 with `?` because the duplicate `Script` source was 2190 chars vs the canonical `LocalScript` 2189 — the only diff was a final `\n`. Brief said "byte-equal or stop"; Codex obeyed exactly. Fix: relaxed the rule to "byte-equal or differs only by trailing whitespace / line endings." Real non-whitespace diffs still stop-and-flag. Decision logged in `_Decisions.md`. Lesson: "byte-equal" is rarely what you want when comparing Lua source — semantically equal is the goal, and Lua's grammar makes trailing whitespace inert.
- **Initial mis-read on change-log drift.** During wrap-up I flagged that PRs #1, #3, #4, #5, #6 looked missing from `_Change_Log.md` — turned out they were already in the per-PR `HH:MM UTC` format, just brought in via PR #7 which I hadn't accounted for yet. Re-read after PR #7's diff resolved the false alarm. No actual drift to backfill.

## 🤔 Decisions

- **Brief equality rule: whitespace-tolerant.** Captured in `_Decisions.md`. Pattern available for future briefs that need source-equality comparisons before destructive action.
- **Drift-found ScreenGuis: keep all three.** `IntroScreen`, `Menu`, `Game Version` stay live and Studio-only. None blocking other work, two have tooling-blocker fidelity issues for clean export, and the third is one tiny LocalScript not worth the export effort.
- **Workspace Manual Export queue: skip.** `Avalog`, `Leader2`, `playerBugReportSystem`, `ReportGUI`, `Truss`, `WelcomeBadge`, `Rose` stay Studio-only. `Workspace` isn't Rojo-mapped so they don't affect repo sync; load-bearing `Avalog` is fine living in Studio while `FavoritePromptPersistence` itself stays no-touch.
- **Next XP follow-up: Title v2.** Spec already exists in `02_Systems/Title_System`. Build planning waits for Tyler to kick off the thread; don't auto-design.

## ▶️ Next Up

- **XP testing-place checks.** Sitting boost (now unblocked since SeatMarkers has a Seat child), level-up animation, gamepass +22 tick, mobile bar height, second-join migration, DataStore failure simulation. Cheapest first; Tyler and Opus walk it together via Studio MCP.
- **Title v2** when Tyler kicks off the thread. Build brief, scope decisions, AchievementTracker dependency story.
- **DiscordLogs Secret Refactor** stays ⏸ Waiting on Tyler's choice of secret-handling approach (Branch A/B/C).
- **Watch flag:** `FavoritePromptPersistence` line-4 SourceCode error did not reproduce in PR #8's playtest. If it stays gone for a few sessions, move it from `_Known_Bugs.md` Active to Resolved with a "did not reproduce after PR #8" note. Investigation still blocked by no-touch + Avalog deferral.

## 📝 Notes

- **Session shape was tight:** decision triage → brief → review-and-merge → integration, in roughly 30 minutes. The bookend routines plus the in-place brief Tyler just merged validate that the workflow does what it was designed to do — Codex builds, Opus reviews in plain English, Tyler merges with confidence, integration captures the trail.
- **AGENTS.md still reads "Opus (Claude, in chat)"** — cosmetic update from session 1's deferral list still hasn't happened. Carried forward from session 2's recap. Should fold into the next AGENTS.md edit pass that touches it for a real reason.
- **Tyler will save a place backup after this session** named `2026-04-27_post-promptfavorite-cleanup.rbxl` (or his preferred convention). Backup captures testing-place state with all 8 PRs landed.
