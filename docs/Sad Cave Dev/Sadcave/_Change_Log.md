# Change Log

> Append-only history of substantive changes to Sad Cave. **Newest entries on top, sorted by event time (not by when the entry was written).**
> Written by Claude during end-of-session integration. One line per change. Date + system + what changed + why.
>
> **Format:** `YYYY-MM-DD HH:MM UTC — [System] — Change. Reason.` Use `HH:MM UTC` when the timestamp is known (PR merges, hard scheduled events). Use `(<session label>)` (e.g. `(Cowork session 1)`, `(pre-Cowork morning)`) when only a session-scoped time is known. UTC timestamps preserve unambiguous ordering across sessions and time zones.
>
> **What belongs here:** shipped features, design pivots, removed systems, renamed concepts, broken assumptions corrected.
> **What does NOT belong here:** stray ideas (→ `08_Ideas_Parking_Lot`), open questions (→ `09_Open_Questions`), session recaps (→ `07_Sessions`), small in-progress tweaks (→ `00_Inbox` until they ship), design decisions with reasoning (→ `_Decisions.md`).

---

## 2026

### April

<!-- New entries go directly below this line. Sort by event time, newest on top. -->

2026-04-29 10:48 UTC — Title_System / NameTag_Status — PR #23 (`codex/title-polish-pass`, head `c098f659`) merged 2026-04-29 10:48:45 UTC. Polished menu + nametag visual pass shipped. Four files rewritten: `src/StarterGui/TitleMenu/TitleMenuController.client.lua` (full rewrite as right-side drawer with six category sections, BindableEvent open/close decoupling, world-dimming overlay via TextButton click-catcher, row diffing on `TitleDataUpdated` keyed off `ownedTitleIds` deltas, `Quart Out 0.3s` open + `Quart In 0.25s` close + `Sine 0.2s` dim fades, all four close paths wired — click outside / ESC via `UserInputService.InputBegan` / internal `x` button / re-click the tab); `src/StarterGui/TitlesToggleButton/TitlesToggleController.client.lua` (full rewrite as slim 18×90px right-edge tab with rotated `titles` text, hover lift to `(1, 4, 0.5, 0)` and background opacity 0.25 → 0.15, fires `OpenRequested`/`CloseRequested` BindableEvents with fallback to direct `Root.Visible` toggle if bindables don't appear within 5s); `src/ServerScriptService/NameTagScript.server.lua` (`ensureBillboardLayout` flipped — `TitleLabel` 16px tall on top with Gotham 11 lowercase warm-grey at TextTransparency 0.25, 2-3px gap, `NameLabel` 28px tall at Position `(0, 0, 0, 19)` with Gotham 16 full warm-grey, idempotent for the Avalog watchdog, BillboardGui height stays at 50); `src/StarterPlayer/StarterPlayerScripts/NameTagEffectController.client.lua` (`glow` rebalanced from `UIStroke` Thickness 1 / Transparency 0.55 to Thickness 2 / Transparency 0.85 / `ApplyStrokeMode = Border` for ambient halo treatment instead of bordered label, `shimmer` gradient endpoints tightened from 0.25/0.9/0.25 to 0.2/0.7/0.2 for lower contrast at the smaller top-position role, `pulse` `brighten` amount reduced from 0.08 to 0.05). 12 achievement hint lines authored verbatim from the brief's § 6 mixed-voice table (`said_something`→`say something to someone`, `sat_down`→`the first time you rest`, `left_a_mark`→`leave a note behind`, `came_back`→`come back another time`, `keeps_coming_back`→`keep finding your way back`, `part_of_the_walls`→`be here long enough to belong`, `heard_them_all`→`hear every voice`, `knows_every_chair`→`find a different seat each time`, `up_too_late`→`stay until the early hours`, `fell_asleep_here`→`rest for a long while`, `one_of_us`→`join the group`, `day_one`→`be here from the start`). Studio cleanup deleted three stale duplicates: `ReplicatedStorage.TitleConfig` id `1_12815` (the brief's authorized cleanup), `ReplicatedStorage.TitleRemotes` id `1_10394` (the brief's authorized cleanup), AND `StarterPlayer.StarterPlayerScripts.NameTagEffectController` id `1_10619` (caught by Codex during validation, was actively running the old glow stroke at 0.55/1 and would have silently overridden the new ambient halo — Codex made the deletion call and documented it; Claude review approved the call post-hoc and added a `_Decisions.md` entry establishing the "Codex may extend cleanup scope when a stale duplicate is actively breaking new behavior, with verify-before-delete + inbox documentation" pattern). Codex Studio playtest used localhost source mirroring (Rojo plugin logged `NetFail` and didn't reconnect to the worktree server) and confirmed: title row above name on spawn/respawn, drawer bindables open to `{1,0},{0.5,0}` and close to `{2,0},{0.5,0}`, six category sections render, `slow_steps` equip updates nametag + `wearing` caption / unequip returns to auto `believer`, all four effect branches apply correctly, console clean (`[NameTag] script ready` / `[TitleService] script ready` only). Validation limits captured: hover, real ESC keypress, real outside-click, real edge-tab click, mobile tap, Avalog destroy path, mid-session unlock row diff weren't physically exercisable through MCP — code paths verified static and runtime object checks pass; Tyler verifies the tactile interactions in normal play. Server contract untouched (no DataStore changes, no RemoteEvent contract changes, no `TitleService.lua` modifications). Reason: implements the full polished-pass design from `02_Systems/Title_System.md` § "Polished Pass — Drawer + Title-Above-Name" + the 9-step Codex brief at `06_Codex_Plans/2026-04-29_Title_Polished_Menu_Nametag_v1.md`. Title v2 player-facing surface is now feature-complete on polish; only category activations remain (Presence next in the activation sequence, then Exploration + Seasonal). Placeholder TitleMenu retired from `_Cleanup_Backlog.md`; duplicate `TitleConfig`/`TitleRemotes` open question moved to Resolved (three duplicates deleted, none remaining).

2026-04-29 (Cowork session 2, 2026-04-29) — Title_System / NameTag_Status — Polished pass design locked. Tyler-led design pass (Cowork session 12, second of 2026-04-29) captured the polished-menu + nametag-visual rework. Five rounds of focused decisions: nametag flips title-above-name (Gotham 11 lowercase warm-grey at 0.75 opacity on top, 2-3px gap, Gotham 16 name underneath; `glow` effect rebalances from UIStroke-border to ambient underglow halo, `tint`/`shimmer`/`pulse` get lower-contrast tweaks for the smaller top-position role); menu becomes a right-side drawer (~34% width, slides in from right with `Quart Out` 0.3s) with the world dimmed-but-visible behind it (no blur, dim to ~75% brightness over 0.2s); category-sectioned inside (Level / Gamepass / Presence / Exploration / Achievement / Seasonal) with owned-then-locked within each section; click-to-commit (no hover-preview); slim edge tab on the right replaces the top-right `titles` button as the open affordance; four close paths (click outside, ESC, internal close button, re-click the tab); locked-row hint voice goes mixed/contextual (mechanical for level/gamepass/presence/exploration, indirect-poetic for achievement and seasonal — 12 hand-written achievement hint lines authored in the brief). Design captured in `02_Systems/Title_System.md` as a new "Polished Pass — Drawer + Title-Above-Name" section; cross-referenced from `02_Systems/NameTag_Status.md` § "Polished Pass — Title-Above-Name" with implementation-order notes for `NameTagScript.server.lua` and `NameTagEffectController.client.lua`. Stale "title sits under your name" text on Title_System line 21 corrected to "title sits above your name like a quiet epigraph." Codex brief queued at `06_Codex_Plans/2026-04-29_Title_Polished_Menu_Nametag_v1.md` (branch slug `codex/title-polish-pass`, 🔵 Queued) — translates the spec into 9 implementation steps including a Studio cleanup step (Step 8) deleting the stale duplicate `TitleConfig` ModuleScript and stale duplicate `TitleRemotes` Folder caught during AchievementTracker review (logged in `_Open_Questions.md`). Full design rationale recorded in `_Decisions.md` (4 entries: title-above-name, right-side drawer with world-visible, mixed hint voice, roll-cleanup-into-polish-brief). Reason: Tyler asked for the polished pass to start. Active Focus item moved 🔵 Tyler-led design session → 🔵 Brief queued for Codex.

2026-04-29 08:52 UTC — AchievementTracker — PR #20 (`codex/achievement-tracker`, head `2df4378`) merged 2026-04-29 08:52:02 UTC. Brief written, Codex implemented + pushed branch, Claude review passed playtest verdict "safe to merge", Tyler merged. Session covered: design pass with Tyler resolving four open questions (`fell_asleep_here` → idle via `Player.Idled`; `heard_them_all` → ship now with min-NPC gate of 3; `knows_every_chair` → per-server-session set; `day_one` → dormant `LAUNCH_WINDOW` placeholders Tyler sets at real launch). Brief at `06_Codex_Plans/2026-04-29_AchievementTracker_v1.md`. Implementation: `ServerScriptService/Progression/AchievementTracker.lua` (444 lines) + `AchievementTrackerInit.server.lua`, `ReplicatedStorage/AchievementRemotes/ClientLocalTime` RemoteEvent, `StarterPlayer/StarterPlayerScripts/AchievementClient.client.lua`, additive `ConversationEnded` BindableEvent on `DialogueDirector`, additive `NoteSubmitted` BindableEvent on `NoteSystemServer` (no-touch system touched under explicit brief authorization with surgical-only edit). `TitleService.lua` extended for achievement-category ownership + new public accessors (`GetTitleData`, `WaitForTitleData`, `SaveTitleData`, `GetProgressionState`); `TitleConfig.lua` gained `HEARD_THEM_ALL_MIN_NPCS = 3`, `LAUNCH_WINDOW = { startUnix = nil, endUnix = nil }`, `GetAchievementTitleIds()`. Claude review playtest live-verified four achievements firing on Tyler's join (`came_back`, `keeps_coming_back`, `part_of_the_walls`, `one_of_us`), gating working for `heard_them_all` (only QuietKeeper exists) and `day_one` (LAUNCH_WINDOW nil), NameTag still rendering Tyler's manually-equipped `familiar face`, no errors in console, XP gamepass multiplier still firing. Six interactive achievements (`said_something`, `sat_down`, `left_a_mark`, `knows_every_chair`, `up_too_late`, `fell_asleep_here`) read clean in code review but require active player interaction to runtime-verify; left for Tyler's normal play. Reason: implements the AchievementTracker brief per the Title category activation sequence (first slot, anchored to days 3–7 of the return arc per `Player_Experience_Arcs`), and resolves the long-standing `fell_asleep_here` focus-vs-idle open question.

2026-04-29 (Cowork session 1, 2026-04-29) — Vault — `_Known_Bugs.md` retirement: `FavoritePromptPersistence` line-4 `SourceCode` error moved from Active to Resolved-by-attrition. Did not reproduce in 9 consecutive playtests (PRs #8 → #14 + Cowork session 10 + 2026-04-29 AchievementTracker review playtest). Most likely Tyler's 2026-04-27 testing-place cleanup removed whatever was racing during initialization. `FavoritePromptPersistence` is a no-touch system; no code change made. Watch flag retired. Reason: bugs that don't reproduce across many sessions are noise in the Active tracker; moving to Resolved keeps the active list focused on real risks.

2026-04-29 (Cowork session 1, 2026-04-29) — Vault — `_Live_Systems_Reference.md` updated: `Workspace.SeatMarkers` count corrected from 1 (the 2026-04-27 audit refresh value) to 8. Tyler placed additional Seats between sessions. Drift caught during the AchievementTracker review playtest reality check. Reason: the `knows_every_chair` achievement (5 unique seats per session) is now actually testable end-to-end, which changes which Studio Test Checklist items Codex / Tyler can verify in playtests.

2026-04-29 (Cowork session 1, 2026-04-29) — Vault — Open Question added: duplicate `TitleConfig` ModuleScripts and duplicate `TitleRemotes` Folders in `ReplicatedStorage`. Live state has TWO of each at edit time — one is Codex-managed (with the new constants), the other is stale (from before the 2026-04-27 cleanup). `WaitForChild` happens to return the new one, so AchievementTracker works correctly, but the duplicate state is fragile. Logged for cleanup as a separate small brief. Caught during the AchievementTracker review playtest. Reason: Studio-state hygiene matters for review reliability — a future Rojo sync hiccup could pick the stale module and silently break ownership resolution.

2026-04-29 ~04:30 UTC (Cowork session 10) — Vision / Vault — Player_Experience_Arcs vision doc landed. New `01_Vision/Player_Experience_Arcs.md` makes the felt-shape map permanent across three horizons: arrival (first 10 minutes), return (days 2–7), loyalty (month 3+). Each horizon describes what the player should feel, what currently happens in the cave, where it thins, and what the horizon needs. Diagnosis section names days 3–7 of the return arc as the weakest stretch — three reasons (low design density, leverage on month-3 conversion gate, actionability via inactive systems). Doc is sibling to `Tone_and_Rules` and `Project_Overview`, indexed under Vision quick links. Includes a "How to use this doc" section so future system proposals are evaluated against it ("which horizon is this for? what does it make the player feel?"). Reason: Tyler asked to "improve some game planning and for the future of the game" tonight; agreed the felt-shape map should be permanent design rather than chat-only sketch so subsequent build sessions can be evaluated against it explicitly. Surface that the rest of tonight's loyalty-arc work hangs off of.

2026-04-29 ~04:45 UTC (Cowork session 10) — QuietKeeper_Memory — New `02_Systems/QuietKeeper_Memory.md` spec. The loyalty-arc centerpiece — NPC memory across sessions, surfaced as conditional dialogue line variants gating on player history. Spec defines 8 tracked fields per player (visit count, last visit, current streak, longest streak, total seated seconds, first visit, recent unlocks FIFO, has-been-outside flag). MVP scope gates only on visit count; remaining 7 fields captured from day one so v2 surfaces (gap detection, streak detection, title acknowledgement, seasonal awareness) don't need a migration later. Surface mechanism: extends existing `DialogueData.Characters.QuietKeeper.return` into a prioritized list of conditional variants with the existing line as fallback. MVP variants gate on visit count thresholds 2 / 3–4 / 5+ / 25+ / 100+. Spec includes seven binding tone rules that any future line author must pass (under 10 words, no numbers, no celebration, default-line-stays-live, higher-tier-can-be-quieter, silence-allowed, no-4th-wall). Dependency: existing `Dialogue_System` needs an extension to support conditional variant lists — that's the build-blocker for the MVP brief. Codex-ready when sequenced. Status: 🔵 Planned. Reason: Tyler greenlit deep-designing this surface tonight as the most distinctive month-3 beat — it weaponizes the QuietKeeper personality that already exists, costs no new content layer to start, and is the kind of thing month-3 players brag about to new players.

2026-04-29 ~04:55 UTC (Cowork session 10) — Vault — Three loyalty-arc stub system specs added at ⚪ Idea status: `02_Systems/Personal_Place.md` (the cave noticing your favorite sit-spot — flowers / particle effects / dialogue references on per-player favorite seats), `02_Systems/Seasonal_Layer.md` (calendar-aware shifts in environment / NPC moods / Seasonal title windows; gates the Seasonal title category activation), `02_Systems/Veteran_Threshold_Content.md` (a small handful of beats only visible after long playtime / return-count — passport-stamp asymmetry between day-1 and month-3 players). Each ~50-line stub includes a one-paragraph idea, rationale for why it serves the loyalty arc, what it would touch, open questions, dependencies, "designed when" trigger, risks, related-doc links. Reason: avoid losing the surfaces from tonight's loyalty-arc inventory while not over-designing them under-time; ⚪ Idea status keeps them tracked through the index alongside everything else without committing to specs that aren't ready. Cleaner than a separate roadmap meta-doc because the index is already the canonical tracking surface for systems.

2026-04-29 ~05:15 UTC (Cowork session 10) — Title_System — Category Activation Sequence rewrite. The "Follow-up briefs (post-MVP-2, recommended order)" section in `02_Systems/Title_System.md` was rewritten in-place into a new "Category Activation Sequence" section anchored to `01_Vision/Player_Experience_Arcs.md`. Old order (polished menu → Achievement → Presence → Exploration → Seasonal → v1 retirement) was a system-readiness ordering; new order is anchored to the felt-shape map: (1) AchievementTracker first because the Achievement category contains the highest concentration of beats falling in days 3–7 of the return arc — the diagnosed weakest stretch — plus session-1 firsts that lead the player into the loop and long-tail beats (`part_of_the_walls`, `fell_asleep_here`) that seed loyalty; (2) Presence second because it's cheap (single counter against thresholds), reinforces days 3–7 from the time-spent angle, and seeds long-tail loyalty at 500hr+; (3) Exploration third, paired with `Outside` build + `Area_Discovery` build (exploration titles need somewhere to explore — wait until the world has 3+ zones); (4) Seasonal last, gated on `Seasonal_Layer` graduating from ⚪ Idea to a real system spec, with the `day_one` title carved out as opportunistic. Polished TitleMenu and v1 retirement repositioned as parallel tracks Tyler picks the timing for; polished menu best slotted *after* AchievementTracker so the menu has 12 more titles to display when it gets its polish pass. Section closes with a "what this sequence does for the felt-shape map" payoff explaining how the four activations close the days 3–7 gap and seed the loyalty arc. Reason: anchoring the activation order to the felt-shape map turns a flat category list into an opinionated build queue with reasoning that will outlive tonight's session, so future sessions can swap the order only by writing a counter-decision in `_Decisions.md` and not by silently reordering.

2026-04-29 ~05:25 UTC (Cowork session 10) — Vault — `00_Index.md` updates. Added `Player_Experience_Arcs` under Vision quick links with a one-line hook ("felt-shape map across arrival / return / loyalty horizons. Read before adding a new system."); added `QuietKeeper_Memory`, `Personal_Place`, `Seasonal_Layer`, `Veteran_Threshold_Content` under Active Systems with statuses (🔵 Planned for QK_Memory, ⚪ Idea for the three stubs); annotated the existing `Title_System` entry with a pointer to the new "Category Activation Sequence" section; expanded the existing "Polished TitleMenu" Active focus item to "Polished TitleMenu + nametag title visual" to capture Tyler's bookmarked concern from earlier in the session ("titles sit in the name like someone actually cares") + added a sentence framing the nametag-visual scope (PR #12's TitleLabel-below-NameLabel pattern is functional but reads as utilitarian; the polished pass should treat the title as part of the player's identity, not metadata stapled below the name). Reason: the index is the navigation surface for everything in the vault; new docs only get used if they're discoverable from here. Polished-nametag scope captured explicitly so it doesn't drop between sessions.

2026-04-29 03:08 UTC — Title_System — PR #17 (`codex/title-v2-migration-verification`, head `e3d11fd`, merge commit `3ad5880`) merged. Runtime verification of the v1 → v2 title migration code path that PR #14 shipped but never exercised live. Closes the static-review gap PR #14's note flagged ("Migration code path was static-reviewed only — production cutover PR will need a runtime migration test"). Single additive change in `main`: 8 lines on `src/ServerScriptService/TitleService.lua` adding `TitleService.LoadAndMigrateForUserId(userId, storeKeyPrefix)`, a small public function that builds a stub `{UserId = ...}` and calls the existing `loadTitleData` path. The optional `storeKeyPrefix` parameter is the brief Section 7 "extra paranoia" namespacing option Codex used during the run; when set, `stub.UserId` becomes a string like `_PROBE_TitleV2Migration_999999991` and `getStoreKey(player)` (which is just `tostring(player.UserId)`) keys both `titleStore` and `titleV1Store` with the prefixed string, fully isolated from real player data. The probe Script itself (`CodexMigrationProbe`) was authored, run, and deleted from both the repo branch and live Studio before push (same pattern as PR #13's `CodexTitleFollowupProbe`) — additive-only diff, no probe code in `main`. Test results captured in inbox: `regular → familiar_face` PASS, `newcomer → new_here` PASS, `saber_owner → new_here` with `migratedTitleId=nil` PASS (correctly fell back via `DEFAULT_TITLE_ID` for unmappable v1 entries). All six post-cleanup `:GetAsync` reads (three `EquippedTitleV1` + three `TitleData`) returned `nil`. `rojo build` passed before and after probe deletion. `migratedFromV1=true` set on every test case — confirms migration is one-shot per the spec. Claude post-push review spot-checked `TitleConfig.MIGRATION` directly: `regular = "familiar_face"` (line 472), `newcomer = "new_here"` (line 464), `saber_owner` not in table → falls back via `DEFAULT_TITLE_ID = "new_here"` (line 5). All three Codex PASS results match what the code would actually produce, not just self-reported. Reason: implements the full brief at `06_Codex_Plans/2026-04-28_Title_v2_Migration_Verification_v1.md` per Cowork session 9's hand-off; closes the production-cutover prerequisite (migration is now exercised at runtime, not just inspected) and leaves `LoadAndMigrateForUserId` in `main` as a reusable surface for the production-cutover brief later. PR was created and merged within ~30 seconds (created 03:08:01 → merged 03:08:12 UTC), so Claude review happened post-merge — the immediate post-merge review found no blockers and no follow-up brief needed.

2026-04-28 08:31 UTC — Title_System — PR #14 (`codex/title-v2-mvp2`, head `12713cb`, merge commit `f8353be`) merged. Title v2 MVP-2 — placeholder TitleMenu + manual equip + production-cutover migration. Five new files + one substantial modification. Created `src/ReplicatedStorage/TitleRemotes/EquipTitle.meta.json` and `UnequipTitle.meta.json` (RemoteEvents, client → server). Created `src/StarterGui/TitleMenu/init.meta.json` (ScreenGui, `ResetOnSpawn=false`, `IgnoreGuiInset=true`, `DisplayOrder=20`) + `TitleMenuController.client.lua` (~12KB) — programmatic ScrollingFrame menu with two TextButton tabs (`owned` / `locked`), color dot per `tintColor`, lowercase title text, owned rows are clickable TextButtons (click-to-equip; clicking the currently-equipped row toggles back to auto-equip), locked rows show category-specific hints (`reach level 50`, `play for 24 hours`, `buy the title pack`, etc.), titles sorted by category then by levelRequired/hoursRequired/display, ESC key closes the menu. Created `src/StarterGui/TitlesToggleButton/init.meta.json` (ScreenGui, `ResetOnSpawn=false`, `DisplayOrder=19`) + `TitlesToggleController.client.lua` — small lowercase `titles` text button anchored top-right at `Position=UDim2.new(1, -60, 0, 24)`, soft warm color, semi-transparent dark background, subtle 0.15s sine-ease hover tween, click directly toggles `playerGui.TitleMenu.Root.Visible`. Modified `src/ServerScriptService/TitleService.lua` substantially: added `equippedManually`, `migratedFromV1`, `lastEquipTime` to player state; new private `equipTitle(player, titleId)` and `unequipTitle(player)` functions wired through `OnServerEvent` in `Start()`, with server-side ownership re-resolution (never trusts client) + 1-second rate limit + titleId type/existence validation; new `migrateFromV1(player, titleData)` reads the `EquippedTitleV1` DataStore once via pcall+retry and maps via `TitleConfig.MIGRATION` (32 mappings), sets `migratedFromV1=true` regardless of outcome; `loadTitleData` invokes migration only when no prior v2 data exists; `saveTitleData` persists the new fields; `refreshAutoEquip` rewritten to split detection from application — new `findNewlyUnlockedTitleId` helper compares previous and next ownedSets, gamepass-priority then highest-level; if `equippedManually` is false and the auto-pick changes, fires the full equip payload via `applyEquip`; if `equippedManually` is true (or auto-pick unchanged) and there's a newly-unlocked title, fires a separate `notificationOnly = true` payload carrying the unlocked-title metadata so the XPBar plays the unlock fade while the equipped state on the nametag stays put; payload now includes `ownedTitleIds` (full sorted list) and `equippedManually` flag for client-side rendering; defensive self-heal if a manually-equipped title becomes un-owned. Bonus work beyond brief: `started` guard on `Start()` for idempotency, `ACHIEVEMENT_HINTS` lookup table with hand-written hint copy for all 12 achievement IDs (instead of the brief's fallback "do something special"), `task.delay(2)` re-fire of the title payload on join (workaround for client-listener-ready race so the menu doesn't sit empty), `UISizeConstraint` on the menu Root (290–620 wide, 280–520 tall) for mobile sizing. `ResetOnSpawn=false` on both ScreenGuis so menu state survives respawns. Codex's playtest validated: clean startup, menu/toggle populated, manual equip and unequip both worked, invalid ownership rejected, spam rate-limited, manual `slow_steps` survived a level 49 → 50 boundary (the regression test for the new manual-vs-auto interaction), Avalog watchdog rebuilt the BillboardGui correctly. Migration code path was static-reviewed only (no fake `EquippedTitleV1` value was written for the live test user) — production cutover PR will need a runtime migration test. Reason: implements the full Title v2 MVP-2 spec from `06_Codex_Plans/2026-04-28_Title_v2_MVP2_v1.md` per Cowork session 8's MVP-2 design walk and the two AskUserQuestion calls (manual equip respects player choice forever; menu entry point is a small lowercase text button in the top-right corner). Title v2's player-facing surface is now feature-complete except for the polished menu pass — placeholder logged in `_Cleanup_Backlog.md` as the swap target. Three carry-forwards captured for the polished-menu brief later: hint copy voice-pass, BindableEvent-decoupling of the toggle button, and rebuild-on-every-event row diffing for efficiency at scale. The achievement / presence / exploration / seasonal title categories remain defined-but-inactive — each will activate via its own follow-up brief (AchievementTracker first).

2026-04-28 07:22 UTC — Title_System — PR #13 (`codex/title-v2-mvp1-followup`, head `3825b74`, merge commit `f0fec8c`) merged. Two architectural cleanups on top of the just-shipped Title v2 MVP-1: (1) **TitleService becomes a ModuleScript.** Renamed `src/ServerScriptService/TitleService.server.lua` → `TitleService.lua` (ModuleScript class), restructured to expose a small public API (`Start`, `GetPlayerTitlePayload`, `ApplyTitlePayloadToBillboard`) with all state and helpers private to the file. Added `started` flag guard so `Start()` is safely idempotent. Added `TitleServiceInit.server.lua` — three-line Script that requires the module, calls `Start()`, prints `[TitleService] script ready` (preserves console parity with PR #12). Updated `NameTagScript.server.lua` to do `local TitleService = require(ServerScriptService:WaitForChild("TitleService"))` directly, replacing the `_G.SadCaveTitleService` lookup pattern. Dropped the `DEFAULT_TITLE_PAYLOAD` constant and `_G`-not-found fallback paths from NameTagScript so any require failure surfaces loudly instead of silently degrading. (2) **Level watcher self-heals on respawn.** Added one line — `attachLevelWatcher(player)` — to the existing `CharacterAdded` handler inside `onPlayerAdded`. The function is already idempotent (disconnects before reconnecting), so it's safe to call repeatedly; if leaderstats survives the respawn the connection lands on the same IntValue, if it gets recreated the connection lands on the new instance. Codex caught a Rojo gotcha not in the brief: changing a file's class (Script → ModuleScript) doesn't make Rojo delete the stale live instance, so the old `TitleService` Script kept hanging around in Studio. Codex manually deleted the orphan and verified `git grep "SadCaveTitleService"` returns zero matches in the repo. Codex's playtest validated the change with a temporary `CodexTitleFollowupProbe` server script: gamepass priority suppressed for the test, set level 9, respawned via `LoadCharacter()`, set level 10, BillboardGui forcibly destroyed for the watchdog test. Result: `beforeRespawn=settling in`, `afterRespawn=still here`, `leaderstatsSame=true`, `watchdogRebuilt=true`, `rebuiltTitle=still here`, then real title `believer` restored. Probe deleted after test. Reason: Claude's post-merge review of PR #12 flagged both items as non-blocking but worth cleaning up before MVP-2 lands so the architecture doesn't compound. Tyler greenlit the cleanup; brief at `06_Codex_Plans/2026-04-28_Title_v2_MVP1_Followup_v1.md` was 🔵 Queued → 🟢 Shipped within the same Cowork session 8.

2026-04-28 06:10 UTC — Title_System — PR #12 (`codex/title-v2-mvp1`, merge commit `1ac4157`) merged. Title v2 MVP-1 — data + display layer of the redesigned title system. Five new files + two modifications. Created `src/ReplicatedStorage/TitleConfig.lua` (566 lines) defining all 56 titles from the spec tables (20 level milestones spanning level 1 → 10000, 6 exploration with `zoneId` slots, 8 presence with `hoursRequired`, 12 achievements with `achievementId`, 4 seasonal with `seasonStart`/`seasonEnd` slots, 6 gamepass under gamepass id `1797105034`); active categories on this ship are level + gamepass, the other category fields are defined for shape stability so AchievementTracker / Discovery / Presence / Seasonal briefs can land without a config refactor. Includes the v1 → v2 migration table (32 mappings) for the future production cutover; testing-place-irrelevant for now since the v1 pipeline was deleted in the 2026-04-27 cleanup. Created `src/ReplicatedStorage/TitleRemotes/init.meta.json` (Folder) + `TitleDataUpdated.meta.json` (RemoteEvent) — the wire surface for client-bound title updates. Created `src/ServerScriptService/TitleService.server.lua` (339 lines) implementing server-authoritative ownership: pcall+retry on every DataStore call (`TitleData` key, separate from `ProgressionData` per the spec), pcall+retry on `UserOwnsGamePassAsync` with conservative "unowned" fallback on persistent failure, `pickAutoEquip` that prioritizes gamepass titles over level titles for the MVP-1 auto-equip-highest constraint, leaderstats Level watcher via `:GetPropertyChangedSignal("Value")` (the non-invasive observation path since `ProgressionService` is no-touch and fires its own `LevelUp` RemoteEvent), `MarketplaceService.PromptGamePassPurchaseFinished` listener for mid-session purchase flow. Created `src/StarterPlayer/StarterPlayerScripts/NameTagEffectController.client.lua` (178 lines) handling tint (static color), shimmer (UIGradient with 3s ping-pong tween), pulse (TweenService brightness oscillation, 1.5s cycle), and glow (UIStroke at 0.55 transparency); `AttributeChanged` listeners restart the effect when a title changes mid-session, `AncestryChanged` cleans up on destruction, Heartbeat reaper handles stale billboards. Modified `src/ServerScriptService/NameTagScript.server.lua`: BillboardGui size `(0, 200, 0, 30)` → `(0, 200, 0, 50)`, NameLabel resized to top 28px with bottom-aligned text, new TitleLabel below at 30px offset / 18px tall / Gotham 12 / soft warm color / stroke 0.7, `ensureBillboardLayout` made idempotent so the Avalog watchdog can safely re-call it. Modified `src/StarterGui/XPBar/XPBarController.client.lua`: `task.wait(2)` → `task.wait(5)` for unlock notification hold (hover-show stays at 2s), `playLevelUp` extended with combined-fade format using `utf8.char(0x2014)` em dash (`level N — new title: X`), new `playNewTitleOnly` for non-level-tied unlocks (gamepass mid-session), 100ms `task.delay` coalescing window with token pattern matching the existing `revealToken` style, removed the line-220 TODO that was the integration point. Codex's playtest at level 558 confirmed `believer` (gamepass title, expected per the gamepass priority rule), BillboardGui sized 200×50, shimmer gradient active, combined fade probe rendered `level 10 — new title: still here`, level-only probe rendered `level 24`, console clean. Reason: implements the full Title v2 MVP-1 spec from `06_Codex_Plans/2026-04-28_Title_v2_MVP1_v1.md` per the design walk in Cowork session 7 (2026-04-28 04:07 → 05:38 UTC). PR was created and merged within 7 seconds at 06:10:51 → 06:10:58 UTC, so Claude review happened post-merge (Cowork session 8 review verdict: ship-quality, two non-blocking notes captured as the PR #13 follow-up brief).

2026-04-28 03:12 UTC — XP_Progression — PR #11 (`codex/xp-followup-fixes`, head `d06753a`) merged. Three changes: (1) `SourceConfig.GAMEPASS_ID` corrected from `2110249546` (a nonexistent asset — `MarketplaceService:GetProductInfo` returned "Item not found in batch response") to `1790063497`, the actual Sad Cave gamepass `2X Levels` Tyler owns. With ownership now resolving correctly, the 1.5× multiplier finally fires — Codex's playtest confirmed all three multiplied rates: `source=active base=15 granted=22`, `source=sitting base=20 granted=30`, `source=afk base=3 granted=4`. (2) `XPBarController.client.lua` `Background.BackgroundTransparency` dropped from `0.85` to `0.55`. The bar at 6px desktop with 0.85 opacity was rendering invisibly against the dark cave at low fill (Tyler at ~20-30% within current level saw only a tiny warm sliver in the bottom-left); 0.55 gives the unfilled portion a faint dark presence so the full-width footprint reads. Background color, Fill transparency, Fill color, height, level-up bumpHeight — all unchanged. (3) `Driver.server.lua` `tickPlayer` log format expanded from `source=%s amount=%d` to `source=%s base=%d granted=%d` with the granted-amount math computed locally from `state.gamepassOwned` and `SourceConfig.GAMEPASS_MULTIPLIER`. Mirrors the math in `ProgressionService.GrantXP` (no-touch) with a sync-warning comment inline. Closes the gamepass-verification gap from PR #10's log format. Reason: PR #10's testing-place walkthrough surfaced the wrong gamepass ID and the contrast-vs-placement diagnostic; Codex investigated cleanly, stopped at the brief's stop-and-flag conditions for both decisions, Tyler delegated both calls to Claude, picks were Background opacity 0.55 + log format with base+granted. Closes the active focus item from session 5 — XP testing-place checks are now substantially exercised: gamepass +22 verified, sitting boost verified, level-up animation reads at the 6→8 ratio, desktop+mobile bar both visible. Remaining items (second-join migration variants 1+2, DataStore failure simulation) deferred — variant 3 implicitly verified by Tyler's normal rejoins, the failure-simulation steps were walked through with Tyler in plain English in case he runs them later. Mobile + desktop bar polish + level-up animation refinement parked in `08_Ideas_Parking_Lot/_Parking_Lot.md` for revisit when more game features ship.

2026-04-28 01:25 UTC — XP_Progression — PR #10 (`codex/xp-testing-bug-sweep`, merge commit `6a9c26b`) merged. Three changes: (1) `PresenceTick.GetTickAmount` reordered to check `state.seatedAt` (with elapsed ≥ `SITTING_THRESHOLD_SECONDS`) before `state.isAFK` — implements Decision A from the 2026-04-27 (Cowork session 5) design call. A player physically seated at a `Workspace.SeatMarkers` descendant now keeps sitting rate (20 XP/tick, ×1.5 = 30 with gamepass) even when their Roblox window loses focus. The 30-second threshold is preserved. (2) `XPBarController.client.lua` desktop `barHeight` bumped from 4 → 6 (mobile already 6, so the `if isMobile` split collapsed to a flat `local barHeight = 6`). Background transparency 0.85, fill transparency 0.6, fill color, level-up `bumpHeight` 8 desktop / 10 mobile — all unchanged. The level-up bump:resting ratio went from 2x (4→8) to 1.33x (6→8); Tyler later verified the level-up beat still reads cleanly. (3) `Driver.server.lua` gained a `tickPlayer(player)` wrapper that calls `PresenceTick.GetTickAmount` to capture source/amount, then `ProgressionService.Tick` to do the grant, then `print("[Progression] tick: source=%s amount=%d player=%s(%d)")` so the system is visible during walkthroughs. Routed through Driver (not ProgressionService) because `_No_Touch_Systems.md` flags ProgressionService as protected. Codex playtest confirmed all three rate states fire correctly: seated+AFK = sitting/20 (XP `230,957 → 230,977`), standing+AFK = afk/3, standing+focused = active/15. No new console errors. Closes the two `_Known_Bugs.md` Active entries from session 5: "XPBar invisible to player despite GUI hierarchy reporting it as rendered" and "XP system grants AFK rate (3) while player is physically seated at a SeatMarker." Codex committed Tyler's design-call vault edits (Decisions, Open Questions, brief amendment) into the same PR — vault state and code state landed coherent. (Backfilled to change log post-session-6 because the session 5 integration vault edits never got pushed to main before Codex's PR #11 branch took over the working tree; redoing the integration here as part of the combined session 5+6 wrap-up.)

2026-04-27 23:20 UTC — NameTag — PR #9 (`codex/nametag-strip-level-row`) merged. Stripped the `LevelLabel`, the `updateLevel` function, and the leaderstats `Level` hooks from `NameTagScript.server.lua`; resized `NameLabel` to fill the BillboardGui (`UDim2.new(1, 0, 1, 0)`) and shrank the BillboardGui from `(0, 200, 0, 50)` to `(0, 200, 0, 30)` to drop the empty bottom region. Avalog `AncestryChanged` watchdog preserved intact. ~25 lines deleted, 8 changed; no new files, no remote-contract changes, no DataStore changes. Codex playtest in `Testing cave` confirmed `[NameTag] script ready`, single `NameLabel` (`vesbus`) above the spawned character with no `LevelLabel`, watchdog re-create on respawn, XPBar still rendering live with `Level=557 / XP=230,685`, no nametag-related console errors. Reason: implements the 2026-04-27 (Cowork session 4) decision "NameTag level row: remove" — the level value already renders in the XPBar, the doubled overhead surface was louder than the tone allows. (Backfilled to change log post-session-4 because session 4 ended without an end-of-session bookend; PR #9 merged after session 3's recap and before this integration pass.)

2026-04-27 (Cowork session 4) — Vault — Reality check: drift found in `NameTagScript`. Live + repo `NameTagScript.server.lua` (byte-equal) renders a BillboardGui with both a `NameLabel` (display name) and a `LevelLabel` (`"level N"` from leaderstats `Level`, updated live via `:GetPropertyChangedSignal("Value")`); the vault docs claimed "name-only minimal version" in `NameTag_Status.md` and `_Live_Systems_Reference.md` and `00_Index.md`. Updated all three docs to describe reality (name + level, with the level row hooked through `leaderstats.Level`). The keep/remove call on the level row is flagged for Tyler as `[O] ?` in the inbox — the same level value also renders in the XPBar, so the doubled surface may be louder than intended for the tone. Side-finding: `search_game_tree` reported 9 instances at depth 1 in `StarterPlayerScripts` vs 8 expected from the audit; flagged as `?` because Cowork's MCP wrapper is summarizing instance results to counts only and I couldn't enumerate names to find the 9th. SeatMarkers `Seat` child confirmed present this pass — already reconciled in PR #6, no fresh drift there. Studio id confirmed `b00d97e0-2ec5-45ea-b49e-22fdff7d8c5b` (matches `Environments.md`).

2026-04-27 (Cowork session 4) — Workflow / `AGENTS.md` + inbox header — Renamed the planner-role mentions from "Opus" / "Opus (Claude, in chat)" / "Opus (Claude in chat)" to "Claude" / "Claude (in Cowork)" throughout `AGENTS.md`, plus the matching wording in the `00_Inbox/_Inbox.md` header preamble. The `[O]` inbox prefix itself was kept (renaming it would break grammar in every existing inbox/change-log/recap entry); both prefix definitions now annotate that the letter is legacy short for "Opus" and is preserved for log continuity. Strictly cosmetic — no workflow rules change. Reason: deferred carryover from Cowork session 1 (when Claude moved off claude.ai chat onto the Cowork desktop tool); Tyler greenlit the rename this session. Older surfaces — past session recaps, prior change-log entries, `_Decisions.md`, `_Review_Template.md`, and the rest of the vault prose — still say "Opus" historically; left those untouched because rewriting history is the wrong move (the rename happened on a date, and the old wording is correct for entries written before that date).

2026-04-27 22:00 UTC — PromptFavorite — PR #8 (`codex/promptfavorite-bugs-cleanup`) merged. Bounded the unbounded `WaitForChild("FavoritePromptShown")` in `PromptFavorite.client.lua` to 10 seconds with a tagged graceful-return warn if the remote is missing; deleted the duplicate `StarterPlayer.StarterPlayerScripts.PromptFavorite` `Script` instance in Studio (its source matched the canonical `LocalScript` modulo one trailing newline — captured the deleted source in inbox before deletion as a recovery path). Reason: eliminate the per-playtest infinite-yield warning (was the priority-low/medium pre-existing bug from `_Known_Bugs.md`) and resolve the audit's PromptFavorite duplicate-name tooling blocker. Server-side `FavoritePromptPersistence` was untouched (no-touch); Codex's read confirmed it creates `FavoritePromptShown` at runtime via `Instance.new` and listens with `:OnServerEvent`, so the bounded-wait fix is correct and a Rojo-static remote stub would have been wrong. Codex playtest reported no infinite-yield warning, normal `[FavoritePrompt]` script logs, no new errors; the unrelated `FavoritePromptPersistence` line-4 bug did not reproduce in this run (noted but bug stays Active — single non-repro doesn't equal fixed, and that system is no-touch).

2026-04-27 21:54 UTC — Workflow / Vault — PR #7 (`codex/live-repo-audit-refresh`, second push on the same branch) merged. Lands the session-2 vault wrap-up to main: 18 vault files (`AGENTS.md` sync-hardening rules — Opus inbox-first, start-of-session GitHub sync, end-of-session validation; plan-file `Status:` convention applied across all `06_Codex_Plans/` files; the audit-refresh plan added; `07_Sessions/2026-04-27_session_2.md` recap; three Known Bugs entries added; `_Decisions.md` introduced with seven 2026-04-27 decisions; `_Change_Log.md` reordered to event-time-newest-first with HH:MM UTC backfills for PRs #1–#6; `_Open_Questions.md` and `_UI_Hierarchy.md` and `_Live_Systems_Reference.md` reconciled with the audit findings; `00_Index.md` refreshed; `_Plan_Template.md` updated). Reason: persist session 2's integration work to the repo. Tyler authored the local commits during session 2 and opened/merged PR #7 today during session 3, so the vault state was already correct locally but only PR #7 actually shipped it.

2026-04-27 ~21:00 UTC — Vault — Reconciled three drift items caught by the audit refresh in PR #6: (a) `Environments.md` Studio id updated `b2f076e7-1056-4618-9ac5-3e41f079a2e4` → `b00d97e0-2ec5-45ea-b49e-22fdff7d8c5b` (verified via `list_roblox_studios` during review), (b) `_UI_Hierarchy.md` updated to reflect `IntroScreen` and `Menu` are still live (not deleted as the cleanup pass claimed) plus a newly-discovered `Game Version` ScreenGui — top-level source-bearing count went 2 → 5; cleanup-removed list entries for `IntroScreen` and `Menu` annotated with "NOT actually removed" strikethrough, (c) `_Live_Systems_Reference.md` updated to reflect `Workspace.SeatMarkers` now has a `Seat` child with `CustomSitAnimScript` (was logged as 0 children — sitting-boost test now unblocked). Reason: the new sync-rules require closing the loop on drift findings during the same session they're caught.

2026-04-27 20:51 UTC — Repo — PR #6 (`codex/live-repo-audit-refresh`) merged. Replaced the frozen `docs/live-repo-audit.md` with a fresh per-container classification of the cleaned testing place. 49 rows classified: 27 Exact, 0 Structurally mapped, 5 Studio-only intentional, 10 Manual export needed, 7 Tooling blockers. Manual export queue: `Rose`, `Game Version`, `Menu`, `Workspace.Avalog` (453-script dependency of FavoritePromptPersistence — load-bearing), `Leader2`, `playerBugReportSystem`, `ReportGUI`, `SeatMarkers` child-script drift, `Truss`, `WelcomeBadge`. Tooling blockers: `NameTag` BillboardGui, `IntroScreen`/`NoteUI` (UI-property fidelity), duplicate-named `PromptFavorite` Script, duplicate Workspace `Model`/`Rig`/`toggle Lantern` roots. Audit also surfaced three vault drift items (Studio id changed, IntroScreen/Menu still live, SeatMarkers child-script) — reconciled in a follow-up vault pass. Spot-checks during review confirmed three Exact entries (`RemoveFF`, `AfkDetector`, `LevelCurve`) byte-equal between Studio and repo.

2026-04-27 ~14:30 UTC — Dialogue_System — QuietKeeper dialogue verified working by Tyler post-PR #5. No formal Opus playtest; Tyler confirmed during Cowork session 2 that conversations trigger and play correctly. Closes the post-merge verification item that was open because PR #5 was pushed with static validation only.

2026-04-27 ~14:30 UTC — 06_Codex_Plans — Live Repo Audit Refresh brief handed to Codex (🔵 Queued → 🟡 Building). Brief was already written this session; Tyler sent it to Codex now. Branch will land as `codex/live-repo-audit-refresh`. The frozen banner in `docs/live-repo-audit.md` (still showing pre-2026-04-27 cleanup state with deleted systems like LevelLeaderstats, TitleService, ShopMenu, etc.) will be replaced once the PR merges.

2026-04-27 ~14:00 UTC — Vault — Cowork session 2: change-log reorder + format hardening + index refresh + plan-status convention. Caught the 2026-04-27 block out of order (PR #2 at 10:13 UTC was sitting above Cowork session 1 entries that happened later) and a prepend-bug that fused the PR #3 entry with leftover text from the Housekeeping brief entry. Reordered the whole 2026-04-27 block by event time, de-mangled PR #3, backfilled missing entries for PR #1 and the pre-Cowork testing-place cleanup pass that were only ever in the inbox. Updated change-log format to require `HH:MM UTC` timestamps where known and explicit session labels otherwise. Reason: Tyler asked for the change log to be ordered newest-first by event time and the workflow to stop losing sync.

2026-04-27 ~14:00 UTC — Vault — `00_Index.md` refresh. Stale Current Priority block (still queuing the repo-strip brief that shipped) replaced with audit-refresh-as-next-up. Recently Completed expanded with PRs #1–#5. Plans & Logs section listed every plan file with explicit status. ⏸ Waiting added to the status legend. Plan-status sync added to end-of-session integration as a mandatory step. Reason: Tyler flagged the index drift; root cause was Cowork session 1's recap being written before Codex executed the housekeeping and dialogue-restore briefs, with nobody resyncing the index after.

2026-04-27 ~14:00 UTC — 06_Codex_Plans — Plan-status convention adopted. Every file in `06_Codex_Plans/` now carries a `Status:` line at the top using the same emoji legend as the index: 🔵 Queued / 🟡 Building / 🟢 Shipped / ⏸ Waiting / ⚫ Superseded. Backfilled status onto all five 2026-04-27 plans (Repo Strip = Shipped PR #2/#3, Housekeeping Export = Shipped PR #4, Restore DialogueDirector = Shipped PR #5, DiscordLogs Secret Refactor = Waiting per Tyler, plus the new Audit Refresh = Queued). Existing 2026-04-25 plans (Repo Resync = Superseded, Live Reconciliation Continuation = Shipped, XP MVP = Shipped) already had statuses. Updated `_Plan_Template.md` so future plans start with the field. Reason: surface execution state in the plan files themselves, not just in the index — keeps the truth co-located with the spec and gives integration a checkable invariant.

2026-04-27 ~14:00 UTC — 06_Codex_Plans — Wrote `2026-04-27_Live_Repo_Audit_Refresh_v1.md`. Codex brief to redo `docs/live-repo-audit.md` from scratch as a fresh per-container classification of the cleaned testing place. Replaces the frozen banner left by PR #4 (Option B). Buckets: Exact / Structurally mapped / Studio-only intentional / Manual export needed / Tooling blocker. Branch: `codex/live-repo-audit-refresh`. Read-only audit (no live changes). Reason: Tyler asked for the audit to be redone since most legacy systems were removed in the 2026-04-27 cleanup, making the old audit's classification stale.

2026-04-27 13:32 UTC — Repo — PR #5 (`codex/restore-dialogue-server`) merged. Restored `src/ServerScriptService/DialogueDirector.server.lua` (679 lines) from the 2026-04-24 Studio-captured hardened source. Closes the dialogue gap created by the cleanup pass — `RequestCharacterConversation` had no server listener after `DialogueDirector` was deleted alongside other legacy systems. Pushed with static validation only (Rojo sync/playtest deferred per Tyler's request); a post-merge playtest of QuietKeeper conversation is now an active focus item. Bound to existing `DialogueData` shape and the four `DialogueRemotes` from PR #4; no DataStore writes; no references to deleted systems.

2026-04-27 11:58 UTC — Repo — PR #4 (`codex/housekeeping-utility-export`) merged. Exported 10 of the 11 Studio-only kept-list objects flagged by PR #2: `DialogueData` (236 lines), `DialogueRemotes` folder + 4 RemoteEvents (PlayCharacterDialogue, PlayPlayerDialogue, PlayerDialogueChoiceSelected, RequestCharacterConversation), `RemoveFF` (7 lines), `Reset` (29 lines), `SoftShutdown` (85 lines), `PromptGroup` (14 lines), `PromptFavorite` (69 lines — note class mismatch flagged in inbox), `NpcDialogueClient` (825 lines), `PlayerDialogueClient` (568 lines). Documented `environment change ` (trailing-space empty Folder) as Studio-only intentional. Did NOT export `DiscordLogs` because its `LogsSettings` child contains a Discord webhook URL marked DO NOT SHARE — the `2026-04-27_DiscordLogs_Secret_Refactor_v1.md` brief addresses that separately and is currently ⏸ Waiting per Tyler. Retired `xp-only.project.json`. Froze `docs/live-repo-audit.md` with a banner (Option B); the freeze is being replaced by the Audit Refresh brief queued in Cowork session 2.

2026-04-27 11:33 UTC — Repo — PR #3 (`codex/repo-strip-to-studio-state`) merged. Follow-up commit on the same branch as PR #2 — small re-push to keep the strip aligned with what Codex's Phase 0 verification found in the testing place. No semantic divergence from PR #2; included for the audit trail.

2026-04-27 (Cowork session 1, late) — Workflow — Five vault improvements adopted in one pass. (1) `AGENTS.md` Re-orientation section replaced with explicit Session Bookends — Start-of-Session, Reality Check, End-of-Session each as a numbered checklist; old informal version retired. (2) New `_Review_Template.md` at vault root documenting the universal Codex-review routine in six steps plus a verdict template; `AGENTS.md` now points at it from a new "Codex Review Template" section. (3) New `01_Vision/Environments.md` explicitly naming the testing place vs production split, what's the same, what's different, and the production cutover protocol; the Start-of-Session bookend reads it when production cutover is in scope. (4) New `_Decisions.md` at vault root for forward-looking design-decision records, separated from the change log which records shipped changes; the End-of-Session bookend appends to both as appropriate. (5) New persistent Cowork artifact `sad-cave-dev-tracker` pulling current priority + active focus from `00_Index`, recent inbox activity, open questions, known bugs, GitHub PRs and commits, and the recent change log on a single auto-refreshing page. Reason: drift between vault / repo / Studio is the workflow's biggest failure mode (clearly visible in the 2026-04-25 → 2026-04-27 gap); making bookends symmetric and explicit, plus a single live status surface, should catch drift while it's small.

2026-04-27 (Cowork session 1, late) — Workflow — Dashboard artifact `sad-cave-dev-tracker` updated. Initial version showed empty sections everywhere because the MCP standard response envelope (`{content: [{type:"text", text:"..."}]}`) wasn't being unwrapped client-side; the parser only handled the simpler `{content: "string"}` shape we'd observed in some tool calls. Fixed unwrap to handle every common shape (raw value, raw string, `{content: string}`, `{content: [{type:text, text:...}]}`), and added a Debug toggle that shows raw response shapes per call so future shape changes are visible in-browser without needing a session round-trip to debug.

2026-04-27 (Cowork session 1, mid) — 06_Codex_Plans — Wrote `2026-04-27_Housekeeping_Utility_Export_v1.md`. Small follow-up brief covering: export 11 Studio-only kept-list objects flagged in PR #2 (DialogueData, DialogueRemotes folder + 4 RemoteEvents, RemoveFF, DiscordLogs, Reset, SoftShutdown, PromptGroup, PromptFavorite, NpcDialogueClient, PlayerDialogueClient, environment change — with branch logic for the trailing-space name); retire `xp-only.project.json`; freeze `docs/live-repo-audit.md` with a banner. Branch: `codex/housekeeping-utility-export`. Reason: PR #2 (which ran ahead of this Cowork session) explicitly left these for review rather than guessing; this brief makes the decisions explicit so Codex can finish the post-cleanup housekeeping in one swing.

2026-04-27 (Cowork session 1, mid) — 06_Codex_Plans — Wrote `2026-04-27_Repo_Strip_to_Studio_State_v1.md` for Codex. Brief is narrower than the chat-only draft from previous session because verification showed the repo `src/` is already lean (the three new artifacts — `NameTagScript`, `AfkEvent`, `AfkDetector` — are already in the repo via prior Rojo sync; the repo doesn't hold the legacy code that needed stripping). Brief covers: confirm three new artifacts match Studio, retire `xp-only.project.json`, freeze or refresh the stale `docs/live-repo-audit.md`, light playtest, push branch.

2026-04-27 (Cowork session 1, mid) — 00_Index — Refreshed to reflect post-cleanup priorities. Active Systems list re-tiered. New "Historical / Superseded" section. New "Reference / Meta" section. Current Priority block updated: land repo-strip housekeeping brief, walk testing-place open questions, pick next XP follow-up. Tools section updated to mention Cowork as host for Claude.

2026-04-27 (Cowork session 1, mid) — 02_Systems — Per-system status reframing post-cleanup. `XP_Progression`: 🔵 Planned → 🟡 Building (MVP shipped). `Level_System`: 🟡 Shipped → ⚫ Superseded by XP_Progression (deleted 2026-04-27). `Daily_Rewards`: 🟢 Shipped → ⚫ Removed (deleted 2026-04-27). `Area_Discovery`: 🟢 Shipped → 🔵 Planned (badge script deleted; will be rebuilt as part of XP Discovery source). `NameTag_Status`: 🟢 Shipped (rebuilt) — spec rewritten to describe the new minimal name-only `NameTagScript` + `AfkEvent` + `AfkDetector` plumbing built 2026-04-27. `Title_System`: 🔵 Planned — noted that v1 was deleted in cleanup, so v2 builds onto a blank slate in testing place; production migration plan still applies if production retains v1. `Cave_Outside_Lighting`: noted Theme override scripts deleted; lighting configs now decorative-only without driver. `Level_Stat_System`: replaced with permanent stub pointing at Level_System / XP_Progression.

2026-04-27 (Cowork session 1, mid) — Vault — Heavy refresh after Tyler's 2026-04-27 testing-place cleanup pass. Multi-doc reconciliation: `_Live_Systems_Reference` rewritten to reflect post-cleanup state, with explicit "Removed in 2026-04-27 Cleanup" audit-trail section. `_UI_Hierarchy` rewritten — most pre-cleanup ScreenGuis (TitleMenu, ShopMenu, dual Menu, Settings, IntroScreen, Custom Inventory, ComputerUI, fridge-ui, SadCaveMusicGui, bruh, TTTUI, NotificationTHingie, ScreenGui orphans) deleted; remaining canonical surfaces are NoteUI and XPBar (others marked "verify"). `_Cleanup_Backlog` reorganized into Completed (most of original list) vs Still Pending (donations decision, dialogue verification). `_No_Touch_Systems` pruned of deleted entries (LevelLeaderstats, TitleService, DailyRewardsServer, AdminServerManager, NameTagScript Owner) and gained entries for `ProgressionService`, `Progression.*` remotes, and `AfkEvent`.

2026-04-27 (Cowork session 1, start) — Workflow — Migrated Claude from claude.ai chat to Cowork desktop tool. Same MCPs are wired up (Roblox Studio, GitHub, sadcave-repo filesystem, Obsidian) plus shell, structured task tracking, and persistent artifacts. Open Cowork question from previous session resolved: switch is fine, no capability lost, several gained. AGENTS.md still reads "Opus (Claude, in chat)"; cosmetic update deferred until next workflow edit.

2026-04-27 10:13 UTC — Repo — PR #2 (`codex/repo-strip-to-studio-state`) merged. Stripped legacy paths from `src/` to match the cleaned testing place: deletions across `ReplicatedStorage` (Admin/Bonk/EButton/NameValue/Remotes/RunValue/Shop*/Spawn/Speed/TipProductConfig/TitleConfig/TitleEffectPreview/TitleRemotes), `ServerScriptService` (AdminGamePass/AntiExploit/CashLeaderstats/Custom Chat Script/DailyRewardsServer/LevelLeaderstats/NameTagScript Owner/OverheadTagsToggleServer/SadCaveMusicPauseData/Shop/ShopService/TextChatServiceHandler/Theme/TitleService), `StarterGui` (currencyui/Custom Inventory/GUIToggle/notificationUI/SadCaveMusicGui/ShopMenu/Teleport Button/TitleMenu), and `StarterPlayer` (Sprint, AFKLS, AutomaticPrompt, camera, ChatBubbleDarkTheme, CoreGuiBackpackDisabler, DisableCoreUI, Levelup, LocalScript, MessageMaker, OldChatBubbleTheme, PromptFavorite.server.lua, RainScript, Theme). Added the three new artifacts: `NameTagScript.server.lua`, `AfkEvent` RemoteEvent, `AfkDetector.client.lua`. Codex flagged 11 Studio-only kept-list objects left out of this PR for review: `DialogueData`, `DialogueRemotes`, `RemoveFF`, `DiscordLogs`, `Reset`, `SoftShutdown`, `PromptGroup`, `PromptFavorite`, `NpcDialogueClient`, `PlayerDialogueClient`, `environment change` — addressed by PR #4 (housekeeping export) and PR #5 (DialogueDirector restore).

2026-04-27 08:45 UTC — Repo — PR #1 (`codex/xp-progression-mvp`) merged. XP Progression MVP shipped: `XPBar` UI + `ProgressionService` + `PresenceTick` + remotes + curve. Live test before merge confirmed XPBar visible, migration recovered Tyler's real level (557, totalXP 230,177), leaderstats Level + XP populate, active tick fires every 60s for +15 XP, boundaries respected. Cutover post-merge flipped `SourceConfig.ENABLED=true` and disabled legacy `LevelLeaderstats` / `Levelup`. (Backfilled to change log during Cowork session 2 — was only logged in inbox; the prior session's integration missed it.)

2026-04-27 (pre-Cowork morning) — Workspace — Tyler did a heavy testing-place cleanup pass: deleted most legacy systems (LevelLeaderstats, Levelup, TitleService/TitleConfig/TitleMenu, ShopService/ShopMenu, AdminServerManager, AreaDiscoveryBadge, Sprint, fridge-ui, ComputerUI, Menu, Settings, IntroScreen, Custom Inventory, BackpackCoreGuiController, Theme, daily rewards, music GUI, etc.). Kept: XP system (Progression folders, XPBar, SeatMarkers), Notes, ToolPickup, PromptFavorite, PromptGroup, FavoritePromptPersistence, ReportHandler, RemoveFF, DiscordLogs, Reset, SoftShutdown, dialogue scripts. Also built three new things: a robust `NameTagScript` (Server, attaches BillboardGui to HumanoidRootPart with an AncestryChanged watchdog because Avalog destroys nametags parented to Head); a `ReplicatedStorage.AfkEvent` RemoteEvent; a `StarterPlayerScripts.AfkDetector` LocalScript that fires AfkEvent on WindowFocused/Released. Plus a `Workspace.ToolPickups` empty Folder so ToolPickupService stops yielding. Repo and vault were not touched during this cleanup; PRs #2–#5 reconciled the repo and vault to the cleaned state. (Backfilled to change log during Cowork session 2 — was logged in inbox 2026-04-27 evening section but never made it to the change log during the prior session.)

2026-04-27 (early morning, pre-PR #1) — XP_Progression — Hardened MVP legacy migration safety on `codex/xp-progression-mvp`: if the legacy `LevelSave` read fails during migration, `ProgressionService` now marks the load as failed so it skips writing a 0-XP `ProgressionData` record. Reason: live testing briefly showed level 0 when the legacy read failed; preserving old level data is more important than completing a partial migration.

2026-04-25 — Workflow — Adopted branch-and-merge workflow ahead of GitHub MCP being connected. Codex now works on `codex/<task-name>` branches and pushes them; never commits to `main` directly. The user merges (one click on GitHub) only after Opus reviews. AGENTS.md updated: new Git Workflow section explaining the branch lifecycle, Loop steps 3 and 5 updated, Build Loop steps 3 and 6 updated, Done When adds branch-pushed bullet, new anti-pattern (Codex pushes to main). Reason: with GitHub MCP, Opus can read pushed branches and give a verdict before anything lands in `main`. Branches are the safety gate; `main` only gets clean reviewed code.

2026-04-25 — Workflow — Three changes after a beginner-perspective audit. (1) Merged `_Workflow.md` into `AGENTS.md` — one workflow doc instead of two paired docs. `_Workflow.md` replaced with a pointer stub so Obsidian still finds it. (2) Dropped the 🔵→🟡→🟢 plan-status mechanic. Codex no longer updates Status fields on plan files; vault write access is now just the inbox. Plan template and the active XP plan file had their Status lines removed. (3) Opus review is now universal — every Codex task gets reviewed by Opus before the user accepts it, no risk gradient. The user does not script and cannot verify code; Opus translates the diff into a plain-English verdict. Changes locked into AGENTS.md (Loop step 5, Build Loop step 6, Done When, anti-patterns). Reason: previous workflow assumed an experienced solo dev who could route their own notes, judge task risk, and read diffs. The user is none of those things — they need Opus to do the routing, judging, and verifying.

2026-04-25 — 02_Systems — Created `_No_Touch_Systems.md`. Migrated the canonical no-touch list out of `AGENTS.md` and into the vault as a sister doc to `_Cleanup_Backlog.md` and `_Live_Systems_Reference.md`. AGENTS.md now points to it instead of carrying the inline list. Reason: static lists in AGENTS.md drift from reality as systems get renamed/added/removed. Vault location means the list gets maintained alongside the `02_Systems/` notes that own those systems.

2026-04-25 — Workflow — Audit pass on `_Workflow.md` and `AGENTS.md`. Six fixes: (1) AGENTS.md → vault pointer for the no-touch list (above). (2) Both docs now declare themselves paired — workflow rule changes update both in the same edit pass. (3) Build Loop step 6 in AGENTS.md gained playtest failure-mode bullets (runtime error → fix-or-flag, MCP unavailable → don't silently skip). (4) Both docs corrected on Rojo — it syncs files to Studio, it doesn't commit; git commits separately. (5) New "Starting a Fresh Session" section in AGENTS.md mirrors Opus's re-orientation checklist for Codex. (6) AGENTS.md "Done When" plan-Status bullet caveated for ad-hoc tasks. Two new anti-patterns in `_Workflow.md`: playtest theater and asset bloat — both with mitigations wired into existing steps. Asset Generation section in AGENTS.md gained a placeholder-tracking note; integration step 6 in `_Workflow.md` gained a placeholder-cleanup-logging line. All changes additive; no role boundaries moved.

2026-04-25 — Workflow — Added Studio MCP tool integration to the loop. Step 1 (Design): Opus may use `execute_luau` for live state queries. Step 3 (Build): Codex now playtests via `start_stop_play` + `console_output` before marking 🟢 — was implicit before, now explicit. Step 5 (Review): Opus reads Codex's playtest notes and only spot-checks if risky or thin. AGENTS.md updated to match — Build Loop step 6 calls the playtest tools by name, Validation section reflects MCP playtest, new Asset Generation section authorizes `generate_mesh` / `generate_material` for placeholders. All changes additive; no role boundaries moved. Reason: Codex playtest capability went live this session; docs caught up.

2026-04-25 — 06_Codex_Plans — Wrote `2026-04-25_XP_Progression_MVP_v1.md`. First design-driven brief on the new system. MVP scope: ProgressionService + PresenceTick + XPBar. Build-and-test phase ships with `SourceConfig.ENABLED = false`; production cutover (Phase 7) flips the flag and disables `LevelLeaderstats` / `Levelup`. Rollback is one boolean. Three open questions left for Codex to resolve during build (AFK detection mechanism, leaderstats Level overlap during cutover, Rojo conventions for empty RemoteEvents).

2026-04-25 — 00_Index — Updated current priority from live reconciliation to XP Progression MVP. Marked Title_System v2 as 🔵 Planned (v1 still live). Marked XP_Progression as 🟡 Building. Added `LevelLeaderstats` and `Levelup` chat notification to cleanup backlog (retired by MVP).

2026-04-25 — Title_System — Full v2 redesign. Scrapped the v1 list (34 level + 30 shop + 15 gamepass + 2 special = 81 titles, many off-tone). Replaced with ~60 titles across 6 categories: level (20), exploration (6+), presence (8), achievements (12), seasonal (4+), gamepass (6). Names all lowercase with spaces, emotional/internal voice (`still here`, `half awake`, `gone quiet`, `always was here`). Every title has a per-title `tintColor` (Color3) for effect rendering. Five effect tiers: none → tint → shimmer → pulse → glow. New `TitleConfig` data shape with category-keyed unlock conditions. New combined DataStore key `TitleData` (equipped title + achievement flags). New `AchievementTracker` module under Progression. TitleMenu simplified to two filter tabs: `owned` + `locked`. Full v1→v2 migration table written. Rationale: v1 was generic-Roblox-rank flavored; v2 reads as a state of mind, not a flex.

2026-04-25 — XP_Progression — Full spec written. Replaces time-only leveling (`+1 level/60s`) with real XP system. Curve: `floor(45 + 0.55 * N^1.15)`. Pacing tuned so level 10 hits in ~27 min, level 100 in ~9 hrs, level 500 in ~2 months, level 1000 in ~8 months. Three-state presence tick: sitting (20 XP) > active (15 XP) > AFK (3 XP) — sitting is the most rewarded behavior, fits hangout-game tone. Gamepass `2110249546` keeps 1.5x multiplier. XP bar: bottom of screen, 4px desktop / 6px mobile, hover/tap reveals numbers, soft level-up glow. DataStores consolidated to two combined keys (`ProgressionData`, `TitleData`) instead of four — fewer reads on join, safer with Roblox rate limits. MVP scope cut to ProgressionService + PresenceTick + XPBar; Discovery/Conversation/Achievement/Title v2 are follow-up briefs.

2026-04-25 — Live Reconciliation — Exported `Theme.server.lua` and `OverheadTagsToggleServer.server.lua` to repo (structurally mapped, 9/9 and 35/35 lines). Both hit the numbered-conversational-output connector blocker, so not byte-exact. `fridge-ui` classified as tooling blocker (AnchorPoint null on centered nodes). Audit updated; highest-priority queue cleared.

2026-04-25 — NameTag_Status — Confirmed `OverheadTagsToggleServer` is a no-op server handler. Overhead tag visibility is client-side only. Server remote kept for backward compatibility. Updated `_Live_Systems_Reference` to reflect this — was previously listed as an unverified assumption.

2026-04-25 — Workflow — Frozen `PLANS.md` as historical-only; consolidated all planning to `06_Codex_Plans/` in the vault. Two surfaces was unnecessary friction; PLANS.md predated the vault and was never the intended go-forward.

2026-04-25 — Workflow — Vault committed to git for the first time. Backed up to `origin/main` after a near-deletion incident. Future drift cannot wipe the vault silently.

2026-04-25 — Workflow — Migrated `docs/project-overview.md`, `docs/game-systems.md`, `docs/ui-hierarchy.md`, `docs/known-bugs.md` into the vault. Single source of truth for text documentation. Originals left as stub pointers.

2026-04-25 — AGENTS.md — Removed `CashLeaderstats` from the no-touch DataStore list. It's slated for removal in cleanup, not a system to preserve. Frozen-not-extended is the correct stance.

2026-04-25 — AGENTS.md — Restored `PLANS.md` and `docs/live-repo-audit.md` references after audit revealed they were the active record of months of live-only reconciliation work. Repo wasn't "stale" — it was a curated subset with documented blockers.

2026-04-25 — AGENTS.md — Fixed file extension convention from `.luau`/`.client.luau`/`.server.luau` to `.lua`/`.client.lua`/`.server.lua` to match what the repo actually uses. Language is still Luau; extension is `.lua` for Rojo tooling reasons.

2026-04-25 — 06_Codex_Plans — Wrote, then superseded, `2026-04-25_Repo_Resync_v1.md`. Brief was based on a wrong premise (treating repo as stale). Replaced with `2026-04-25_Live_Reconciliation_Continuation_v1.md` targeting the audit's actual top priorities: `fridge-ui`, `Theme`, `OverheadTagsToggleServer`. Lesson preserved in the superseded brief's frontmatter.

2026-04-25 — Vault — Migrated Codex instructions from `_Codex_Instructions.md` (vault) into `AGENTS.md` (repo root) per session 1's plan. Codex's rules now live next to the code Codex edits.

---

## Format

`YYYY-MM-DD HH:MM UTC — [System] — Change. Reason.`

Use `HH:MM UTC` when the timestamp is known. Use `(<session label>)` (e.g. `(Cowork session 1, mid)`, `(pre-Cowork morning)`) for entries scoped only to a session. **Sort newest on top by event time, not by when the entry was written** — out-of-session work (Codex executing briefs, automated merges) often happens between sessions and slots into the timeline above older session-1 entries.

Examples:
`2026-04-27 13:32 UTC — Repo — PR #5 merged. Restored DialogueDirector. Closes the dialogue gap from the cleanup pass.`
`2026-04-27 (Cowork session 1, mid) — 02_Systems — Per-system status reframing post-cleanup.`
`2026-04-25 — Dialogue_System — Bumped default cooldown 3s → 4s. Felt rushed in playtest.`
