# Open Questions

> Unresolved design questions that need your decision before they can be integrated into a system note. Persistent (unlike `00_Inbox`, this doesn't empty at session end).
>
> **Lifecycle:** moved here from inbox during integration when an item is `?`-flagged and not ready to resolve. Stays here until you decide. Once resolved, the answer is written into the relevant `02_Systems/` note and the question is removed from this file.

---

## Active

<!-- Format: bullet with the question + context + which system it affects -->

- XP_Progression testing-place cutover checks still to exercise: sitting boost, level-up animation, gamepass +22 tick, mobile bar height, second-join migration variants, and DataStore failure simulation. Context: XP MVP branch live test validated XPBar, migration to level 557, leaderstats, active tick, and boundary safety, but these cases still need a formal pass with Opus during the post-merge testing-place walkthrough. **Sitting boost is now unblocked** — `Workspace.SeatMarkers` has a `Seat` child with `CustomSitAnimScript` (verified during the 2026-04-27 audit refresh).
- DiscordLogs Secret Refactor brief is ⏸ Waiting for Tyler to greenlight the secret-handling approach (Branch A: `HttpService:GetSecret`, Branch B: Studio-only sibling ModuleScript, Branch C: script attribute placeholder). Until then, `DiscordLogs` stays Studio-only. Tyler 2026-04-27: revisit some other time, no decision needed yet.
- `StarterPlayerScripts` instance count drift caught during Cowork session 4 Reality Check: `search_game_tree` reports 9 instances at depth 1, but the audit (PR #6 + PR #8 dedup) lists 8 expected (`AfkDetector`, `MobileLightingCompensation`, `NpcDialogueClient`, `PlayerDialogueClient`, `PromptFavorite` LocalScript, `PromptGroup`, `SunRayRemove`, `environment change ` Folder). Cowork's MCP wrapper summarized instance results to counts only on this pass, so the 9th item couldn't be enumerated by name. `script_search "PromptFavorite"` returned `count:1` confirming PR #8's dedup stuck — so it's not a duplicate PromptFavorite. Could be a benign tool-counting quirk (parent counted, etc.) or a real 9th item. Resolve in the next session that has working enumerate (or via a `script_grep` workaround that lists names instead of counts).
---

## Resolved (recent)

> Keep the last few resolved questions here for context. Older ones can be deleted — `_Change_Log.md` holds permanent history.

- 2026-04-27 — AFK-vs-sitting design call (caught Cowork session 5 walkthrough). Tyler delegated the call to Claude. Decided **Branch A — seated SeatMarker overrides AFK**. `PresenceTick.GetTickAmount` will check `state.seatedAt` (with elapsed ≥ `SITTING_THRESHOLD_SECONDS`) before `state.isAFK`, so a player sitting on a SeatMarker keeps sitting rate even when their Roblox window loses focus. Smallest, safest fix that aligns with Sad Cave's tone (presence-rewarding) and avoids the misfire that demoted a seated player to AFK rate during normal alt-tab. Branch B (idle-time gate in AfkDetector) and Branch C (leave as-is) rejected — see `_Decisions.md` 2026-04-27 — "XP system: seated SeatMarker overrides AFK." Implementation lives in [[../06_Codex_Plans/2026-04-27_XP_Testing_Place_Bug_Sweep_v1]].
- 2026-04-27 — XPBar visibility (caught Cowork session 5 walkthrough). Tyler delegated the call to Claude. Decided **bump desktop `barHeight` from 4 to 6** in `XPBarController.client.lua` (mobile already 6, so the if/else effectively unifies). Background and Fill transparencies and warm-tint color stay unchanged — only the resting bar height moves. Reason: bar was rendering correctly per Codex's probe + screenshot, but at 4px against the dark cave it visually disappeared during a full session; 6px restores ambient visibility while staying subtle and premium. See `_Decisions.md` 2026-04-27 — "XPBar visibility: small height bump to 6px desktop." Implementation lives in [[../06_Codex_Plans/2026-04-27_XP_Testing_Place_Bug_Sweep_v1]].
- 2026-04-27 — NameTag level row keep/remove. Tyler decided **remove** — the level value already renders in the XPBar and the doubled overhead surface was louder than the tone allows. PR #9 (merged 2026-04-27 23:20 UTC) implemented the strip; `NameTag_Status.md` is now 🟢 Shipped name-only. See `_Decisions.md` 2026-04-27 — "NameTag level row: remove."
- 2026-04-27 — Keep/delete decisions for three drift-found ScreenGuis (`IntroScreen`, `Menu`, `Game Version`). Tyler decided **keep all three as-is** — they stay live and Studio-only, no export, no cleanup. See `_Decisions.md` 2026-04-27 — "Drift-found ScreenGuis: keep all three" and `_UI_Hierarchy.md` updated drift section.
- 2026-04-27 — Manual Export queue from the 2026-04-27 audit refresh (`ReplicatedStorage.Rose`, `Workspace.Avalog`, `Workspace.Leader2`, `Workspace.playerBugReportSystem`, `Workspace.ReportGUI`, `Workspace.Truss`, `Workspace.WelcomeBadge`). Tyler decided **skip** — `Workspace` isn't mapped in any Rojo project file so these don't affect repo sync, and the load-bearing `Avalog` dependency is fine living in Studio while `FavoritePromptPersistence` itself stays no-touch. Audit doc keeps the list as informational. See `_Decisions.md` 2026-04-27 — "Workspace Manual Export queue: skip."
- 2026-04-27 — Pick the next XP follow-up to design. Tyler decided **Title v2** — full v2 spec already exists in `02_Systems/Title_System`, ready for build planning when Tyler kicks off the thread. Discovery / Conversation / AchievementTracker deferred. See `_Decisions.md` 2026-04-27 — "Next XP follow-up: Title v2."
