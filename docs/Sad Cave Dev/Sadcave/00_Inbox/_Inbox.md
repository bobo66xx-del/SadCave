# Inbox

> Unsorted captures from the current session. Empties at end of session.
> **You**, **Claude**, and **Codex** all write here. One line per entry, timestamped.
>
> **Prefixes:**
> - `[U]` — written by you
> - `[O]` — written by Claude (the letter is legacy short for "Opus" and is preserved so older inbox/change-log entries still read cleanly)
> - `[C]` — written by Codex
> - `?` — unresolved, needs your decision before integration
>
> **Lifecycle:** items get triaged into `02_Systems/`, `_Change_Log.md`, `_Decisions.md`, `_Known_Bugs.md`, or `09_Open_Questions/` at end of session, then cleared from here. Anything still unresolved stays with a `?` prefix and rolls to next session.
>
> **Claude rule (per AGENTS.md Section 4):** before any vault edit during a session, write an `[O]` line first stating what you're about to change and why.

---

<!-- New session entries go HERE — add a new dated section above this line, like:
## 2026-04-28
- [C] 09:15 — first observation
-->

## 2026-04-27 (Cowork session 6)

- [C] 21:08 — Started `2026-04-28_XP_Followup_Fixes_v1` on branch `codex/xp-followup-fixes`; resolved handoff docs onto updated `main` and applied Phase 1's one-line `SourceConfig.GAMEPASS_ID` fix (`2110249546` -> `1790063497`).
- [C] 21:14 — Phase 2 investigation: `StarterGui.XPBar` already has `IgnoreGuiInset=true`; runtime `ScreenInsets=DeviceSafeInsets`. During desktop Play, `workspace.CurrentCamera.ViewportSize.Y=937`, `GuiService:GetGuiInset().Y=58`, and `XPBar.Background` was at `AbsolutePosition.Y=873`, `AbsoluteSize.Y=6`, bottom `879` (`937 - 58`). That means the bar is at the bottom of Roblox's GUI coordinate space, not pushed below the visible area.
- [C] 21:14 — Phase 2 occlusion tests: `PlayerGui` bottom-band objects included `XPBar.Background/Fill`, `Game Version.MainFrame`, and `Menu.MainFrame`; temporarily raising `XPBar.DisplayOrder` to 100 and disabling `Menu`/`Game Version` did not change the placement. Temporarily forcing `Fill.Size.X.Scale=1` made the warm strip render full-width at the same position/color/transparency, so the Fill frames are not being clipped or covered.
- [C] 21:14 — ? Phase 2 stop condition hit: the desktop bar is on-screen and not occluded; the invisibility is likely that normal progress only fills the left ~30% while the unfilled background is too subtle against the cave. Any next fix would be a visual/contrast/background treatment, which the brief says requires a Tyler tone call before changing.
- [C] 21:14 — Phase 1 verification note: `MarketplaceService:GetProductInfo(1790063497, GamePass)` returns Sad Cave gamepass `2X Levels`, and `UserOwnsGamePassAsync(1132193781, 1790063497)` returns true; old `2110249546` still returns item-not-found. Watch flag for later: current tick debug log prints the pre-multiplier source amount from `PresenceTick`, so console still says `amount=15` even when ownership is true unless the log itself is changed or XP delta is used for verification.
- [C] 21:25 — Applied amended Decisions C/D: XPBar resting `Background.BackgroundTransparency` is now `0.55` only, and `Driver.server.lua` tick logs now print `base` plus computed `granted` using the same multiplier math as `ProgressionService.GrantXP`.
- [C] 21:33 — Phase 3 playtest in `Testing cave`: fresh Play showed XPBar `Background` at `2439x6`, `BackgroundTransparency=0.55`, fill scale `0.373` at low fill, and screenshot showed the bottom bar footprint. Console ticks for gamepass owner `vesbus` showed `source=active base=15 granted=22`, `source=afk base=3 granted=4`, and `source=sitting base=20 granted=30`. No new Sad Cave runtime errors observed; one `AssistantCommand` infinite-yield warning was caused by my rejected validation probe against server-only state, not game code.
- [U] — Post-PR-#10 testing in `Testing cave`: sitting/active/AFK rates all confirmed (20/15/3 — Branch A working). Level-up animation reads cleanly at the new 1.33x bump:resting ratio. Per-tick `[Progression] tick:` debug log firing in console as designed. Hover stat (`level N - X/Y xp`) works on both desktop and mobile.
- [U] — Desktop XPBar still not visibly rendering despite the height bump. Tyler's read: this is a placement / occlusion problem, not a transparency problem. Reasoning: if the bar were just too transparent, he'd at least see it faintly; the fact that it's fully missing while the LevelLabel reveals on hover (and the LevelLabel sits 16px above the bar, anchored to the same screen-bottom) means the Background + Fill frames specifically are being covered or pushed off-screen. Mobile (Studio mobile emulator) renders the bar correctly with the same code, so it's desktop-specific.
- [U] — Gamepass not multiplying. Tyler owns the Sad Cave gamepass; ticks still log as 15/20/3 not 22/30/4. Probe via `MarketplaceService:UserOwnsGamePassAsync(1132193781, 2110249546)` returned `false`; follow-up `MarketplaceService:GetProductInfo(2110249546, GamePass)` returned "Item not found in batch response" — `SourceConfig.GAMEPASS_ID = 2110249546` is not a valid gamepass asset. Tyler confirmed the real Sad Cave gamepass ID is `1790063497`. The 2110249546 number was wrong from the MVP / data error.
- [O] — About to write `06_Codex_Plans/2026-04-28_XP_Followup_Fixes_v1.md` covering both fixes: (1) one-line `SourceConfig.GAMEPASS_ID` change `2110249546 -> 1790063497`; (2) investigation+fix for desktop XPBar placement — Codex screenshots in Play, probes `XPBar.Background.AbsolutePosition/AbsoluteSize` to confirm the bar is on-screen, identifies what's covering or displacing it on desktop, fixes appropriately (likely an `IgnoreGuiInset` toggle, position lift, or DisplayOrder/ZIndex bump). Branch: `codex/xp-followup-fixes`. Also updating `00_Index.md` Active Focus to point at the new brief.
- [O] — Noting the `checkRemoteAgainstAllowList: a remote was added to a protected container with non-allowlisted name 'ExpChatFeatureValueChanged'` console warning that Tyler asked about: this is a Roblox engine warning, not from any Sad Cave script. Roblox's internal chat system is trying to add a remote called `ExpChatFeatureValueChanged` and Roblox's anti-exploit allowlist is blocking it. Has been showing up in console output since at least PR #8's playtest; not affecting anything in Sad Cave. Safe to ignore.
- [C] — Phase 1 (gamepass ID) applied in working tree: `SourceConfig.GAMEPASS_ID = 1790063497` set. Verified: ID is valid + owned by `vesbus`. Phase 2 investigation completed and stopped pre-commit per the brief.
- [C] — Phase 2 findings: (a) XPBar Background `AbsolutePosition.Y=873, height 6, bottom 879`; `ViewportSize.Y=937` with Roblox GUI inset 58 → 879 IS the expected on-screen bottom. (b) `IgnoreGuiInset=true` already on the XPBar ScreenGui. (c) Raising `XPBar.DisplayOrder` and disabling `Menu` / `Game Version` ScreenGuis did not reveal a hidden bar. (d) Forcing `Fill.Size.X.Scale=1` made the warm strip render full-width — frames are not clipped or covered. Conclusion: bar is rendering correctly, the issue is contrast — at ~30% fill the bar looks like a tiny warm sliver in the bottom-left while the unfilled portion (just the dark Background) blends into the cave. Tyler's placement hypothesis is rejected by evidence; visual fix is needed after all. Stopped per brief Phase 2 step 7 ("flag if the diagnosis surprises and turns out it IS contrast — Tyler tone call before any visual restyle").
- [C] — Extra finding: per-tick `[Progression] tick:` log prints the pre-multiplier `amount` from `PresenceTick.GetTickAmount`. Once gamepass kicks in, `ProgressionService.GrantXP` applies the 1.5× multiplier to the actual grant (`amountToGrant = math.floor(numericAmount * multiplier)`), but the log line will still show the unmultiplied number. Future gamepass verification requires either an XP-delta probe or a log adjustment.
- [O] — Tyler delegated both calls to me. Locking in: (1) **Bar contrast: drop `Background.BackgroundTransparency` from `0.85` to `0.55`.** Reason: at low fill (~20-30%), the warm Fill is just a tiny sliver and the rest of the bar (the dark Background at 0.85 opacity on a dark cave) is invisible. Bumping the Background to 0.55 gives the unfilled portion a faint dark presence so the bar's footprint reads across the full width regardless of fill level — same color `(15, 15, 15)`, same height (6px), same Fill (warm tint at 0.6). Conservative — addresses the "invisible at low fill" case directly without making the bar feel HUD-y when fill is high. Easy walk-back to `0.7` if `0.55` feels heavy in real play; outside range `0.5–0.7` is the band. Rejected dropping `Fill.BackgroundTransparency` (axis 2) because that only intensifies the filled portion; doesn't fix the problem at low fill. (2) **Tick log shows both base + granted amounts:** change the format from `[Progression] tick: source=%s amount=%d player=...` to `[Progression] tick: source=%s base=%d granted=%d player=...` where `granted = math.floor(base * (state.gamepassOwned and SourceConfig.GAMEPASS_MULTIPLIER or 1))`. Reason: makes gamepass verification trivial — Tyler can see the multiplier firing at a glance without doing math on XP deltas. Note for Codex: this duplicates the multiplier math in `ProgressionService.GrantXP` (which is no-touch); add a one-line comment in `tickPlayer` so the duplication is documented. About to amend the brief with a Section 0 ("Decisions made") block so Codex can finish both fixes plus push without another stop, and log both decisions in `_Decisions.md`.

## 2026-04-27 (Cowork session 5)

- [C] 19:20 — Started `2026-04-27_XP_Testing_Place_Bug_Sweep_v1` on branch `codex/xp-testing-bug-sweep`; read the brief, XP progression spec, known bug entries, no-touch list, tone rules, and relevant progression/XPBar scripts.
- [C] 19:20 — `_No_Touch_Systems.md` still lists `ProgressionService` as protected; routing the required per-tick debug print through `Driver.server.lua` instead of editing `ProgressionService.lua`.
- [C] 19:23 — XPBar playtest probe: `Players.vesbus.PlayerGui.XPBar` is enabled, `Background` is `2439x4` at the bottom edge, and `Fill` is `2022x4` (`Size.X.Scale=0.829`). Screenshot shows a very thin warm strip at the bottom; no controller error found.
- [C] 19:23 — ? XPBar likely needs a Tyler visual call, not a code bugfix: current style is 4px tall with `BackgroundTransparency=0.85` and `Fill.BackgroundTransparency=0.6`. Bumping height/opacity would be visible-player-facing restyle, so I am not changing it without approval.
- [C] 19:28 — Added driver-side tick logging and playtested it: console now emits `[Progression] tick: source=active amount=15 ...`, then `source=sitting amount=20` after sitting on `Workspace.SeatMarkers.Seat`.
- [C] 19:28 — Confirmed AFK-vs-sitting diagnosis: while `Humanoid.Sit=true` and `SeatPart=Workspace.SeatMarkers.Seat`, firing the same AFK signal as `WindowFocusReleased` changed the next tick from sitting to `[Progression] tick: source=afk amount=3 ...`; XP delta was `230839 -> 230842`.
- [C] 19:28 — ? Need Tyler design call before code fix per brief: Branch A = seated SeatMarker overrides AFK; Branch B = tighten AFK detection with idle-time gate; Branch C = leave AFK-overrides-sitting as intentional and document later.
- [C] 19:45 — Resumed after Section 0 decisions: applied Branch A by checking `state.seatedAt` before `state.isAFK` in `PresenceTick.GetTickAmount`; changed XPBar resting `barHeight` to 6 only, with transparency/color/tween/bump values unchanged.
- [C] 19:53 — Playtested amended bug sweep in `Testing cave`: XPBar `Background.AbsoluteSize` is `2439, 6` and screenshot shows the 6px bottom strip; no XPBar controller errors appeared.
- [C] 19:53 — Playtested Branch A: seated on `Workspace.SeatMarkers.Seat`, forced `AfkEvent(true)`, next tick stayed `[Progression] tick: source=sitting amount=20 ...`; XP moved `230957 -> 230977`.
- [C] 19:53 — Playtested fallbacks: standing/non-seated with `AfkEvent(true)` logged `source=afk amount=3`; after `AfkEvent(false)`, standing active logged `source=active amount=15`. No new console errors beyond the existing Roblox protected remote warning and existing FavoritePrompt/pass logs.
- [O] — Tyler delegated the call to me. Two picks made: (1) **AFK-vs-sitting = Branch A.** A seated SeatMarker overrides AFK in `PresenceTick.GetTickAmount`. Reason: tone alignment — Sad Cave rewards presence, and the most explicit presence signal in the game is "I sat down at a meditation seat." Window focus is a weak proxy by comparison. Smallest, safest fix: one conditional reorder. The "abuse case" of farming seated XP while alt-tabbed isn't a real problem — no leaderboard race, no economy that breaks at +17 XP/min over AFK rate, and the entire game philosophy is "time spent existing matters." Branch B's idle-time gate adds state for marginal benefit and could be a follow-up if non-seated AFK detection ever needs tightening; Branch C leaves the feature nonfunctional in real play since players alt-tab constantly during normal gaming. (2) **XPBar visibility = barHeight 6 on desktop** (currently 4; mobile already 6). Reason: bar IS rendering per Codex's probe + screenshot, but at 4px with `Background.BackgroundTransparency=0.85` it's the same color as the dark cave at the bottom of the screen — Tyler couldn't see it during a full session. 6px keeps it minimal (still half the level-up bumpHeight of 8) and unifies with mobile. Keeping `Background.BackgroundTransparency=0.85` and `Fill.BackgroundTransparency=0.6` and the warm-tint Fill as-is — only changing the height. About to log both decisions in `_Decisions.md`, move the AFK question from Open Questions Active to Resolved, and amend the brief with a Decisions block at the top so Codex can finish prong 2 and add the height bump.

- [O] — Walkthrough started: XP testing-place sitting-boost check. Tyler reports sitting on `Workspace.SeatMarkers.Seat`, server probe confirms `Humanoid.Sit=true` and `SeatPart=Workspace.SeatMarkers.Seat`. Installed a client-side XPUpdated listener via `_G.SC_TickLog` to capture tick deltas without server access (execute_luau is client-context during Play).
- [O] — Walkthrough finding #1: tick fired with `delta=+3, level=557, total=230,730` at elapsed=22s. That's `PRESENCE_AFK_XP` (3), not `PRESENCE_SITTING_XP` (20) or `PRESENCE_ACTIVE_XP` (15). Means `state.isAFK=true` on the server even though `Humanoid.Sit=true` and `seatPart` is a SeatMarker descendant. PresenceTick.GetTickAmount checks AFK FIRST and returns AFK rate before considering sitting. Almost certainly caused by Tyler's Studio window losing focus while he's been typing in Cowork — `AfkDetector.client.lua` fires `WindowFocusReleased` → server sets isAFK=true; `WindowFocused` should clear it but evidently isn't (Tyler's been mostly in Cowork, focus rarely returns to Studio for the AFK-clear event to fire). Open design question for Tyler: should "physically seated at a SeatMarker" override window-focus AFK? Sad Cave is presence-rewarding and the player at a meditation seat who briefly checks Discord arguably should not collapse to AFK rate.
- [O] — Walkthrough finding #2: Tyler reports "the xp bar vanished a long while back." Probe shows `PlayerGui.XPBar` is `Enabled=true`, `Background` Frame present at the screen-bottom anchor with `Size=(1,0,0,4)` and `BackgroundTransparency=0.85`, `Fill` Frame at 78.6% scale-X with `BackgroundTransparency=0.6` and `Visible=true`, `LevelLabel` and `HoverDetector` present. The bar is rendering as a 4px semi-transparent strip at the bottom of the screen with a 78.6%-filled warm tint. Either it's so subtle Tyler stopped noticing it, OR another ScreenGui is overlaying it (`Menu` ScreenGui is also enabled with 5 children — possible cover). Codex should screenshot during Play to ground-truth.
- [O] — Walkthrough finding #3: console output is quiet — only the initial-join logs (`[NameTag]`, `[FavoritePrompt]`) appear, and `[NameTag] script ready` shows the strip shipped. No XP-tick logs because `Driver.server.lua`, `ProgressionService`, and `PresenceTick` don't `print()` per tick (only `warn()` on retry-failed paths). Not a bug — but it makes the system invisible during walkthroughs. Small enhancement: a `[Progression] tick: <source> +<amount> player=<userid>` debug print on each `Tick()` call would help Tyler see what's happening live without an MCP probe.
- [O] — Tyler's call: "have codex run through it and fix everything." About to: (a) append the three findings to `_Known_Bugs.md` Active; (b) write `06_Codex_Plans/2026-04-27_XP_Testing_Place_Bug_Sweep_v1.md` with the three-prong investigation+fix scope; (c) update `00_Index.md` Active Focus to point at the bug-sweep brief instead of the live walkthrough; (d) add an Open Question for the AFK-overrides-sitting design call (Tyler-decision, not a code fix).

## YYYY-MM-DD (template — do not write here, this is just a format example)

- [O] HH:MM — example: noticed `CashLeaderstats` still referenced in three places, see `_Cleanup_Backlog`
- [U] HH:MM — example: dialogue cooldown felt too short, bumped to 4s
- [U] HH:MM — ? should daily reward streak break on 1 missed day or 2?
