# Inbox

> Unsorted captures from the current session. Empties at end of session.
> **You**, **Claude**, and **Codex** all write here. One line per entry, timestamped.
>
> **Prefixes:**
> - `[U]` — written by you
> - `[O]` — written by Claude (the letter is legacy short for "Opus" and is preserved so older inbox/change-log entries still read cleanly)
> - `[C]` — written by Codex
> - `?` — unresolved, needs your decision before integration
>
> **Lifecycle:** items get triaged into `02_Systems/`, `_Change_Log.md`, `_Decisions.md`, `_Known_Bugs.md`, or `09_Open_Questions/` at end of session, then cleared from here. Anything still unresolved stays with a `?` prefix and rolls to next session.
>
> **Claude rule (per AGENTS.md Section 4):** before any vault edit during a session, write an `[O]` line first stating what you're about to change and why.

---

<!-- New session entries go HERE — add a new dated section above this line, like:
## 2026-04-28
- [C] 09:15 — first observation
-->

## 2026-04-28

- [O] 04:07 UTC — Re-oriented (Cowork session 7). Walked the start-of-session bookend: AGENTS.md, 00_Index, prior inbox (empty post-session-6), session 6 recap, GitHub sync. PRs #1–#11 all in sync with main — every PR has a `_Change_Log.md` entry with HH:MM UTC, every plan file carries a Status: line, 00_Index Plans & Logs section matches. No new PRs since PR #11 (merged 2026-04-28 03:12 UTC). Vault clean.
- [O] 04:08 UTC — Reality Check for Title v2 prep. **No v1 title surface in Studio:** StarterGui has only the 5 kept ScreenGuis (NoteUI, XPBar, Game Version, IntroScreen, Menu); ReplicatedStorage and ServerScriptService have zero "title"-named instances; `script_grep "TitleService"` returned 1 match — a forward-looking TODO at `XPBarController.client.lua` line 220 (`-- TODO: Listen to TitleRemotes.TitleDataUpdated for new-title fades when TitleService v2 ships.`) which is helpful pre-wiring, not drift. Other `Title*` string matches in the data model are all incidental (TextLabels in unrelated UIs, plugin/CoreGui chrome, the Avalog package's onyx-ui TitleBar). Slate is genuinely blank.
- [O] ? 04:09 UTC — Drift: `AGENTS.md` line 276 ("Live Project Structure" section) still claims "`TitleMenu` is now locally managed through Rojo from `src/StarterGui/TitleMenu`," but PR #2 deleted that path. `src/StarterGui/` now only contains `XPBar/`. Stale doc; should be edited at integration. Not blocking Title v2 design.
- [O] 04:09 UTC — Studio instance id this session: `f4e93190-edb3-47b6-afa8-ef4309840c95` (different from the `b00d97e0-...` recorded in `Environments.md`). Not real drift — Studio mints a new id every launch. May want to reframe `Environments.md`'s id field as "transient per-launch" rather than tracked, but optional; flagging for awareness only.
- [O] 04:10 UTC — Tyler picked "walk the v2 spec first" as the entry point. Starting Title v2 spec walkthrough in plain English; capturing any adjustments here as we go.
- [O] 04:25 UTC — Title v2 walk: Section 1 (shape, categories, effects, lived experience) and Section 2 (level arc, presence/exploration/achievements/seasonal/gamepass categories, names, pacing) both approved. Title-on-nametag stays — sanity check confirmed; PR #9's strip-the-level-row reasoning ("level is also in XPBar, doubled surface is loud") doesn't transfer to titles since titles aren't shown elsewhere and they're closer to a name than a level. Names flagged for slightly different textures (`day one`, `one of us`, `believer`) kept as-is — variation acceptable. `up too late` kept as-is despite the late-night-play tone question.
- [O] 04:25 UTC — Title v2 design call (Tyler): `fell_asleep_here` AFK threshold changes from 30 minutes → 19 minutes. Reason: Roblox auto-disconnects idle players at 20 min, so 30 min is unreachable in normal play. 19 min lands just inside the disconnect window. Editing `02_Systems/Title_System.md` (Achievements table + AchievementTracker hooks list) and appending to `_Decisions.md`.
- [O] ? 04:26 UTC — Implementation nuance surfaced by the 19-min change: the existing `AfkDetector` fires on window-focus loss (`WindowFocused` / `WindowFocusReleased`), but Roblox's 20-min auto-disconnect is idle-based (no input). For `fell_asleep_here` to actually mean "fell asleep at the keyboard," AchievementTracker probably wants idle detection (`Player.Idled` or input-timestamp tracking) rather than the focus-based AfkDetector. Note this on the AchievementTracker brief when written.
- [O] 04:42 UTC — Title v2 design call (Tyler) on the level-up + title-unlock collision: merge into a single combined fade reading `"level N — new title: X"` (e.g. `"level 10 — new title: still here"`). One fade, one moment. Sequencing rejected as feeling too segmented for what is essentially one milestone.
- [O] 04:42 UTC — Title v2 design call (Tyler) on hold duration: bump unlock notifications from 2s → 5s for both level-ups and title unlocks (and the combined fade above). The 2s spec'd value was too short to actually land. Hover-show behavior on the XPBar (level/progress reveal on hover) stays at 2s. Will require an XPBar follow-up brief to retime the currently-shipped level-up animation from its current hold to 5s.
- [O] 04:43 UTC — TitleMenu v2 build approach (Tyler): ship a functional placeholder during the MVP slice; defer the polished menu design to a separate focused session Tyler will run later. Reason: the data pipeline (earn → equip → display on nametag) doesn't depend on menu polish, and visual UI design works better with focused unhurried time + real data flowing through than with speculative mockups. Placeholder will be tracked explicitly in `_Cleanup_Backlog.md` when built so it doesn't quietly become permanent. Polished TitleMenu remains its own brief in the build order (already step 8 in the spec).
- [O] 04:55 UTC — Title v2 MVP brief sizing (Tyler): split into MVP-1 (`TitleConfig v2` + `TitleService v2` for level + gamepass categories with auto-equip-highest, no manual equip; `TitleRemotes.TitleDataUpdated` only; NameTag v2 with title row; XPBar update for combined fade + 5s hold) and MVP-2 (`TitleService` equip/unequip extension; `EquipTitle` / `UnequipTitle` remotes; placeholder TitleMenu; production-cutover migration code). Polished TitleMenu confirmed as a separate later brief that drops in over the placeholder. Reason: smaller per-brief blast radius, easier playtest scope, and the half-feature mid-state ("auto-equip your highest level title only") is a complete visible feature in itself just constrained. Migration is testing-place-irrelevant (testing place has no v1 data) but lands in MVP-2 alongside the manual-equip surface so production cutover becomes one config flip away. Replacing the spec's "Combined build order" section with the post-XP-shipped MVP-1/MVP-2 sequence; appending the call to `_Decisions.md`.
- [O] 05:05 UTC — Tyler picked "write MVP-1 brief now" as the next move. Drafting `06_Codex_Plans/2026-04-28_Title_v2_MVP1_v1.md` against the locked-in spec + build order. Brief covers: TitleConfig v2 data module (all 60 titles defined; level + gamepass actively checked), TitleService v2 (ownership resolution + auto-equip-highest + DataStore TitleData write; no equip/unequip yet), TitleRemotes/TitleDataUpdated, NameTag v2 (BillboardGui 30→~50, lowercase title row, tintColor + effect rendering re-pointed at v2 config), XPBar update (combined `level N — new title: X` fade at 5s, retime existing level-up animation, wire TitleDataUpdated listener at the existing line-220 TODO). Status will land at 🔵 Queued. Will also update `00_Index.md` Plans & Logs section to include the new brief.
- [O] 05:35 UTC — Architectural call captured during brief drafting: `ProgressionService.lua` is no-touch and is what fires the `LevelUp` RemoteEvent (line 409). Cannot extend the LevelUp payload server-side. Two non-invasive paths: (a) TitleService observes `Player.leaderstats.Level:GetPropertyChangedSignal("Value")` to detect level changes server-side, then fires its own `TitleDataUpdated` event; (b) client-side coalescing of the two events in XPBarController via a 100ms `task.delay` window with a token pattern. Both used in MVP-1: (a) for the server-side trigger, (b) for the client-side combined-fade rendering. Documented in the brief Section 3.
- [O] 05:38 UTC — MVP-1 brief written and queued at `06_Codex_Plans/2026-04-28_Title_v2_MVP1_v1.md`. Status: 🔵 Queued. `00_Index.md` Plans & Logs updated with the new brief; Current Priority block reframed to "Title v2 MVP-1 brief queued — awaiting Codex," with MVP-2 noted as the follow-up. Walkthrough complete; ready for Tyler to either hand the brief to Codex now or wrap and ship to Codex in a fresh session.
- [C] 00:33 — Started `codex/title-v2-mvp1`; read MVP-1 brief, Title_System, _No_Touch_Systems, and Tone_and_Rules. Scope is TitleConfig/TitleService/TitleRemotes, nametag title row/effects, and XPBar title-fade coalescing; ProgressionService remains read-only.
- [C] 00:56 — Built Title v2 MVP-1 in repo: TitleConfig has every concrete spec-table entry (56 total: 20 level, 6 exploration, 8 presence, 12 achievement, 4 seasonal, 6 gamepass), TitleService auto-equips level/gamepass titles, NameTag has the title row, and XPBar coalesces title unlock fades.
- [C] 00:56 — Validated with Rojo build + Studio playtest. At level 558, test user auto-equipped gamepass title `believer` (expected because gamepass priority beats level titles), NameTag size was 200x50 with TitleLabel below NameLabel, shimmer gradient was active, combined XPBar probe showed `level 10 — new title: still here`, level-only probe showed `level 24`, and console had no new TitleService/NameTag/XPBar errors.

## 2026-04-28 (Cowork session 8 — re-orient + post-merge review + integration + MVP-2)

- [O] (re-orient) — Walked the start-of-session bookend: AGENTS.md, 00_Index, prior inbox (carrying session 7's unintegrated entries + Codex's PR #12 build record now visible because Tyler pulled main locally after the merge), session 6 recap (most recent on disk; session 7 ended without a recap), GitHub sync. **Found PR #12 (`Add Title v2 MVP1`) merged 2026-04-28 06:10:58 UTC** — vault still had the brief at 🔵 Queued. PR was created and merged inside 7 seconds, so no Claude review happened pre-merge. Tyler greenlit a post-merge review + full integration sweep + roll into MVP-2.
- [O] (re-orient) — Spec ground-truth: `Title_System.md` tables sum to 56 titles (20+6+8+12+4+6); the "~60" header is rounded. PR #12 shipped 56 — no missing titles.
- [O] (re-orient) — Post-merge review of PR #12: looks good, ship-quality. Two non-blocking notes: (a) Codex used `_G.SadCaveTitleService` (brief option b) instead of the recommended ModuleScript pattern — works but order-sensitive, NameTag could briefly fall back to defaults if it runs before TitleService initializes; (b) leaderstats Level watcher attached once at PlayerAdded — fine if ProgressionService keeps leaderstats persistent across respawns (standard pattern), worth a single playtest probe to confirm. Neither blocks.
- [O] (re-orient) — Tyler asked to handle both notes asap before MVP-2. Pivoting to write a small Codex follow-up brief at `06_Codex_Plans/2026-04-28_Title_v2_MVP1_Followup_v1.md` covering: (a) convert TitleService to a `TitleService` ModuleScript + `TitleServiceInit` Script pair, point NameTagScript at `require(...TitleService)` instead of `_G.SadCaveTitleService`; (b) re-attach the leaderstats Level watcher on `CharacterAdded` so it survives any leaderstats teardown/recreate during respawn. Brief will land 🔵 Queued. Updating `00_Index.md` Plans & Logs to include the new brief.
- [O] (re-orient) — Local working-tree note: `C:\Projects\SadCave\src\ReplicatedStorage\TitleConfig.luau` exists locally with `.luau` extension, but PR #12 added `TitleConfig.lua` (`.lua`). Tyler reported Rojo was finicky. Likely a stale Rojo-generated artifact pre-merge. Flagged for Tyler to clean up locally if Rojo behaves oddly on next sync. Not editing the working tree from here.
- [C] 02:15 — Started `codex/title-v2-mvp1-followup`; read the follow-up brief, `Title_System`, `NameTag_Status`, `_No_Touch_Systems`, and `Tone_and_Rules`. Scope stayed server-side: TitleService module split, NameTag require path, and CharacterAdded level-watcher reattach.
- [C] 02:15 — Repo change: moved `src/ServerScriptService/TitleService.server.lua` to `TitleService.lua` ModuleScript, added `TitleServiceInit.server.lua`, removed `_G.SadCaveTitleService`, and updated `NameTagScript.server.lua` to require the module directly.
- [C] 02:15 — Studio sync cleanup: because `ServerScriptService` ignores unknown instances, Rojo kept the old live `TitleService` Script after the class swap. Deleted only that stale live Script; live Studio now has `NameTagScript` Script, `TitleService` ModuleScript, and `TitleServiceInit` Script. `SadCaveTitleService` grep is clean in repo and Studio.
- [C] 02:15 — Validated with `rojo build` and Studio playtest. Console printed `[NameTag] script ready` and `[TitleService] script ready` with no new TitleService/NameTag errors; nametag was 200x50 with `NameLabel` 200x28, `TitleLabel` 200x18, title `believer`, effect `shimmer`.
- [C] 02:15 — Server probe validation: temporary `CodexTitleFollowupProbe` suppressed gamepass priority only during playtest, set level 9 then respawned with `LoadCharacter()`, set level 10, and destroyed the BillboardGui. Result: `beforeRespawn=settling in`, `afterRespawn=still here`, `leaderstatsSame=true`, `watchdogRebuilt=true`, `rebuiltTitle=still here`, then restored real title `believer`. Temporary probe deleted after test.

## YYYY-MM-DD (template — do not write here, this is just a format example)

- [O] HH:MM — example: noticed `CashLeaderstats` still referenced in three places, see `_Cleanup_Backlog`
- [U] HH:MM — example: dialogue cooldown felt too short, bumped to 4s
- [U] HH:MM — ? should daily reward streak break on 1 missed day or 2?
