# Workflow — moved

The workflow doc and Codex rules are now combined into one file at the repo root: **`AGENTS.md`** (`C:\Projects\SadCave\AGENTS.md`).

Why combined: maintaining two paired docs (one for Opus, one for Codex) created sync overhead with no benefit — both audiences read the same workflow. One doc, one source of truth.

Open `AGENTS.md` for:

- The Stack (roles)
- The Loop (6 steps from idea to shipped)
- Vault folder map
- Codex's rules (build loop, conventions, no-touch, validation)
- Re-orientation checklist
- Anti-patterns

This file kept as a pointer so the vault still has a "where's the workflow?" answer.
